# SoundSpot システム設計・仕様書（ロジック＆運用ポリシーまとめ）

本書は、スタジオ横断検索アプリ「SoundSpot」において策定・合意された**スクレイピング方針、BAN回避（人間化）ロジック、キャッシュ運用ルール、スタジオ別の連携仕様、UI/UXデザイン方針**を網羅・整理したリファレンスドキュメントです。

---

## 目次
1. [サービス思想・役割分担](#1-サービス思想役割分担)
2. [スクレイピング＆BAN回避「人間化」ポリシー](#2-スクレイピングban回避人間化ポリシー)
3. [キャッシュ運用設計（静的データ vs 動的データ）](#3-キャッシュ運用設計静的データ-vs-動的データ)
4. [スタジオ別データ仕様・連携方針](#4-スタジオ別データ仕様連携方針)
   - [サウンドスタジオノア (NOAH)](#41-サウンドスタジオノア-noah)
   - [スタジオペンタ (PENTA)](#42-スタジオペンタ-penta)
5. [UI / タイムライン設計ロジック](#5-ui--タイムライン設計ロジック)
6. [エリア別スタジオ調査＆スクレイピング実現可能性マップ](#6-エリア別スタジオ調査スクレイピング実現可能性マップ)
7. [各スタジオの会員登録状況＆最長予約枠・クローリング期間対照表](#7-各スタジオの会員登録状況最長予約枠クローリング期間対照表)
   - [7.1 会員登録状況＆システム特性一覧](#71-会員登録状況システム特性一覧)
   - [7.2 店舗別・最長予約枠 vs 実クローリング取得期間対照表](#72-店舗別最長予約枠-vs-実クローリング取得期間対照表)
8. [主要ファイル・コード構成マップ](#8-主要ファイルコード構成マップ)
9. [アクセス解析・イベント計測設計 (Google Analytics 4)](#9-アクセス解析イベント計測設計-google-analytics-4)

---

## 1. サービス思想・役割分担

当サービスは、各スタジオのWeb予約を乗っ取ったり自動で予約完了まで行うツールではなく、**「都内の複数スタジオを横断し、空き状況と設備を素早く見渡すレーダー」** として位置付けます。

| レイヤー | 役割 |
|---|---|
| **SoundSpot (当アプリ)** | ・渋谷・新宿などエリア内の各スタジオ（ノア、ペンタ等）の空き状況を横断検索<br>・帖数・料金・常設機材（Marshall, JC-120等）での絞り込み<br>・希望する時間帯の「アタリ」を数秒でつける |
| **各スタジオ公式システム** | ・最終的な空き枠の確定・決済・予約完了<br>・当アプリの各部屋カードやタイムラインから「公式で予約 ↗」を押すと、該当スタジオの正規予約画面へダイレクト遷移する |

> **利点**:
> 二重予約や決済トラブルのリスクをゼロにし、利用規約やセキュリティ面でも安全なアグリゲーター構造を維持します。

---

## 2. スクレイピング＆BAN回避「人間化」ポリシー

ノアなど公式予約システムからデータを取り続けるにあたり、**「相手のサーバーに負荷をかけず、bot判定（IP制限・アカウント凍結）を100%回避する」** ための厳格な運用ルールです。

### ① バックグラウンド定時巡回型（ユーザー連動アクセスは禁止）
- **ユーザーが画面を検索・リロードしても、外部スタジオのサーバーへ直接アクセスさせない。**
- 画面に「🔄 最新に更新」ボタンを設けると、ユーザーの連打やアクセス集中によってスタジオ側から「異常なアクセス頻度（bot）」と見なされてしまうため、**手動更新ボタンは設置しない**。
- クラウドクローラー（GitHub Actions）が指定時刻にのみ巡回し、結果をSupabaseおよびキャッシュに直接同期する。

### ② 人間らしく見せる「6大ステルス（人間化）ロジック」
機械的・不自然なアクセスログを残さず、スタジオ側のWAFやbot検知システムによるIP/アカウント制限（BAN）を未然に防ぎ、外部サーバーへ無用な負荷をかけないため、`scripts/crawl-studios.ts` に以下の厳格な安全防護ロジックを完全実装・稼働させています。

| No | ロジック名 | 実装仕様・理由 |
|---|---|---|
| **1** | **毎日固定4回の指定時刻巡回（過剰アクセスの完全排除 ＆ キュー混雑回避）** | **【毎日固定 4回】06:33, 11:48, 17:18, 21:33 JST**<br>人の生活リズム（起床・昼休み・終業後・就寝前）に合わせた自然なタイミングでのみアクセスし、相手先サーバーへの不要な負荷を完全に排除。平日・休日の複雑な分岐を排して毎日定時巡回に一本化。GitHub Actionsのcronが世界的に集中する「00分・15分・30分・45分」のキリ番を回避して数分オフセット（端数化）することで、実行遅延やスキップを未然に防止。 |
| **2** | **ゆらぎ（Jitter）の付与** | `xx:00:00`, `xx:30:00` のようなキリ番の秒境界で即座にアクセスしない。<br>リクエスト前に **ランダムな待機時間（1〜5秒）** を動的に挿入し、手動ブラウジング特有の不規則なアクセス間隔を再現。 |
| **3** | **ログインセッション自動修復 ＆ 完全再利用** | 有効なセッション情報（`storageState.json`）を優先再利用。<br>万が一セッション切れを検知した場合は、純粋Node fetchにより `SysMembers/login/true` からCSRFトークンを取得し、環境変数（`NOAH_LOGIN_ID`, `NOAH_PASSWORD`）で**自動再ログイン＆Cookie自己更新**を行う。 |
| **4** | **フェイルセーフ保護（既存データの維持）** | セッション失効やログイン失敗、通信エラー等が発生した際でも、**既存キャッシュスロットを保持・維持**し、空き枠データが画面から全消去される事故を100%防止。 |
| **5** | **店舗間の人間的アクセスペーシング** | 複数店舗を巡回する際、ミリ秒単位での同時連打を行わず、適度な待機時間を挿入。<br>一括バッチリクエストに見えるスパイクを防ぎ、人間の閲覧ペースを再現。 |
| **6** | **セーフアボート（即時安全停止）** | 万が一HTTP 403（アクセス遮断）等を検知した場合は、**即座に処理を中断し無謀な連続リトライを行わない**。 |

### ③ GitHub Actions による完全クラウド自動巡回 ＆ Vercelビルド枠消費ゼロ化アーキテクチャ
自宅PCの常時起動に頼らず、GitHub Actions と Supabase を直結させたサーバーレス運用です。

- **ワークフロー定義**: `.github/workflows/crawl-studios.yml`
- **実行スケジュール（JST基準）**:
  - **毎日固定 4回**: 06:33, 11:48, 17:18, 21:33 (JST)
  - ※世界中のcronジョブが集中する「毎時00分・15分・30分・45分」のピークタイムを避け、数分の端数を設けてキュー遅延・ドロップを防止。
  - ただしGitHub Actionsのスケジュール実行はベストエフォートで、平均3時間規模の遅延が実測されている（2026-09-20時点）。以前はアプリ側に「発火予定時刻から一定時間ズレたら巡回自体をスキップする」ガードがあったが、この遅延幅に対して非現実的に狭く、大半の定時実行で巡回が丸ごと未実行になっていたため撤廃した。頻度の担保はcronの4エントリ自体に委ねている。
  - `workflow_dispatch`: 手動でいつでも即座に巡回可能。
- **Secrets 連携**:
  - `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
  - `NOAH_LOGIN_ID`, `NOAH_PASSWORD`: ノア自動再ログイン用認証情報
- **Vercel ビルド枠消費ゼロ化（アーキテクチャ刷新）**:
  1. クローラーが取得した都内28店舗・296部屋の最新空き枠データを、Supabase の `availability_slots` テーブルへ直接一括Upsert同期。
  2. Git へのコミットには `[skip ci]` を付与（Vercelの自動フルビルドをスキップ）。
  3. フロントエンド（`src/app/page.tsx`）は `/api/studios` 経由で最新DBデータを動的に取得。初期描画時はローカルキャッシュをフォールバックとして即座に描画（表示遅延・ちらつきゼロ）。
  4. ➔ **Vercelの月間ビルド枠（300回等の制限）の消費を完全に「ゼロ」に抑えつつ、定時巡回による自動データ更新を実現！**

- **GitHub Actions 自動休止防止 ＆ フェイルセーフ運用ポリシー**:
  - `continue-on-error: true`: 万が一特定スタジオの外部APIで一時的な通信障害が発生した場合でも、ワークフロー全体が `failure` 扱いにならず、GitHub の「連続失敗によるスケジュール自動休止（サスペンド）」を完全に防止。
  - `Promise.allSettled`: クローラー内部でスタジオ巡回を並行実行し、一部スタジオの例外時でも成功した他スタジオのデータを安全に保存・同期完了。
- **クローリング実行分数と月間枠の試算**:
  - **1回あたりの所要時間**: 約20〜30秒（1分未満切り上げで **1分 / 回**）
  - **1日の消費分数**: 4回 / 日 × 1分 = **4 分 / 日**
  - **1ヶ月（30日）の消費分数**: 4分 × 30日 = **約120 分 / 月**
  - ➔ GitHub Actions無料枠（**2,000 分 / 月**）の **わずか6%** のみ消費。無料枠の94%を温存。

---

## 3. キャッシュ運用設計（静的データ vs 動的データ）

スタジオ情報の性質に応じて、保存期間（TTL: Time-To-Live）を2層に分離します。

```mermaid
flowchart TD
    subgraph 静的データ (Static Data)
        A1[部屋名・帖数]
        A2[常設機材: アンプ/ドラム]
        A3[基本料金表 / 個人練習料金]
        A4[住所・電話番号・アクセス]
        A1 & A2 & A3 & A4 -->|半月〜1ヶ月に1回更新| DB1[(静的データベース)]
    end

    subgraph 動的データ (Dynamic Data)
        B1[特定日のカレンダー空き枠]
        B2[◯時〜◯時の予約済みステータス]
        B1 & B2 -->|バックグラウンドで約30分おき更新| DB2[(短期キャッシュ)]
    end
```

| データ種別 | 対象データ | 更新サイクル | 運用方針・保存先 |
|---|---|---|---|
| **静的スペック** | 部屋構成、帖数、アンプ/ドラム型番、基本料金、電話番号 | **半月〜1ヶ月に1回** | 内装工事や機材変更は滅多にないため、頻繁なクローリングは行わない。<br>**Supabase（`studios`, `rooms`, `room_equipments` テーブル）** に永続保持。 |
| **動的空き状況** | 各日付・各時間帯の予約枠（空き / 予約済） | **約30分おき** | 上記の「6大ステルスロジック」に基づき、GitHub Actionsからバックグラウンドで穏やかに更新。<br>**Supabase（`availability_slots` テーブル）** へ直接Upsertし、Next.js APIルート（`/api/studios`）経由でクライアントへ即時配信。 |

> **デモ用ダミーデータの完全排除方針**:
> 初回プロトタイプ検証用に使用していたダミースタジオ（吉祥寺・千葉・柏）および乱数ハッシュによる架空スロット生成ロジックは**完全に撤廃**しました。
> また、初期プロトタイプ開発時の残骸データであった架空の「スタジオペンタ 新宿南口店」や初期モックUUID・不要コンバーターを完全削除しました。
> 現在SoundSpotに登録・表示されるスタジオは、都内5エリア（渋谷 8店舗・新宿 4店舗・秋葉原 5店舗・高田馬場 5店舗・池袋 6店舗）の**実在する28店舗・296部屋の公式スペックおよび実データ（公式API・公開カレンダー巡回・リアルタイム空き状況ボード・電話受付）のみ**で純粋に構成されています。

> **池袋エリア追加（2026-09-17）**:
> ノア池袋店（11室・ノア公式API `scripts/scan-noah-branch.ts ikebukuro`＝branch_id 13で実データ確認）、ベースオントップ池袋西口店（7室・studi-ol.com shop_id 2355、旧北口店は閉店済のため西口店に対応）をリアルタイムクロール対応で追加。スタジオペンタ池袋店（5室）・池袋ハンズサイド（8室）・Vivo Sound Studio（4室）は電話/LINE予約のみのため、渋谷ペンタ・BAZOOKA STUDIOと同様の静的リスティング（`src/lib/penta-converter.ts`・`src/lib/vivo-converter.ts`）として掲載。

> **ゲートウェイスタジオ 池袋北口店 追加（2026-09-17）**:
> ユーザーからの追加依頼を受け、gw-studio.com公式サイトを調査。5F（Ast〜Gst・7室）と6F（1st〜8st + SUBROOM・9室）の2フロアが同一tel・同一Reserve1カレンダー（`lc=tlsccmeco&mn=3&gr=1`）を共有する構成で、渋谷ゲートウェイ（3F・4F・5F統合）と同じ「1店舗・複数フロア」の扱いとして計16室を統合。各フロアの「料金」ページ（`/studios/studio_iken(5f)/price`）と実カレンダーの部屋ラベル・開始オフセットを直接突き合わせて確認済み。営業時間は公式記載どおり10:00〜23:00（土日9:30〜）で24時間営業の記載は無い。
>
> **ゲートウェイ全店の24時間営業表記を訂正（2026-09-17）**: 上記の池袋実装時、渋谷・高田馬場3号店は「予約状況により24時間対応可」として`is_24hours: true`扱いにしていたが、ユーザー指摘を受け公式サイトを再確認したところ、渋谷道玄坂店・高田馬場3号店とも「基本営業時間 10:00~23:00」のみが明記されており24h対応の記載は無いことが判明。`src/lib/supabase/api.ts`のis24Hours判定は当初「店舗名に『ゲートウェイ』を含む」ことをノアと並ぶ24h店舗の簡易ヒューリスティックとしていたが、ゲートウェイ全店で24h実績が無いため、ノアのみを対象とする判定に変更した。あわせて`gateway-shibuya-real.json`・`gateway-takadanobaba-real.json`・`scripts/crawl-studios.ts`・`src/config/supported-studios.ts`の該当箇所（`is_24hours`/`is24Hours`フラグと営業時間表示テキスト）を実際の公式サイト記載（10:00〜23:00）に修正した。

> **高田馬場エリア追加（2026-09-15）**:
> NOAH高田馬場店・ゲートウェイ高田馬場3号店・スタジオ音楽館 馬場駅前店・BASS ON TOP高田馬場店の4チェーンをリアルタイムクロール対応で追加。BAZOOKA STUDIOはreserve1.jp（ReserveMart）を採用しているが会員登録者以外は空き状況を閲覧できない仕様のため、自動巡回非対応の静的リスティング（部屋・機材情報のみ、`src/lib/bazooka-converter.ts`）として掲載。
> ゲートウェイ高田馬場3号店は他のReserve1系店舗と異なりカレンダー表の起点が10:00で、部屋ごとに00/15/30/45分の4種類の開始オフセットが混在する特殊な店舗だったため、`scripts/lib/reserve1-fetcher.ts`に`openHour`設定と汎用端数フィラー処理（`koma_spN`）を追加して対応。Supabaseの`rooms.start_time_offset`のCHECK制約も`(0,30)`から`(0,15,30,45)`に拡張した（`supabase/migrations/20260905000000_initial_schema.sql`）。
> スタジオ音楽館 馬場駅前店は公式には「全8スタジオ」だが、通常のバンド練習予約フロー（`ReservationStaff.php`）で実際に予約枠として出現するのはstaffid 2〜7の6室のみ（45日間・全時間帯を走査して確認）。staffid 1・8は「23畳」スタジオライブ特別枠等、通常の時間予約グリッド外での運用と推測され、クロール対象から除外している。

> **`rooms.id` ⇔ `availability_slots.room_id` の採番方式統一（2026-09-15）**:
> `supabase/seed.sql`（`rooms`/`room_equipments`の静的マスター）と、クローラーが書き込む`availability_slots.room_id`は、以前は別々の方法でUUIDが割り当てられており、Gateway渋谷以外のほぼ全店舗でIDが一致せず外部キー制約違反でスロット同期が静かに失敗していた（`/api/studios`がSupabase由来の「部屋はあるがスロットが空」のデータをそのまま返してしまう問題も併発）。
> `scripts/lib/id-utils.ts`の`toUUID(canonicalId)`に採番方式を一本化し、各クローラー（Gateway・Penta等に存在した二重プレフィックスのバグも含め）が使う`canonicalId`文字列と、`supabase/seed.sql`が`rooms.id`を生成する際の入力文字列を完全に一致させた。今後、部屋を追加・変更する際は各スタジオのマスターデータ（`src/config/noah-master.ts`や各`*-real.json`）の`id`フィールドを変更したら、`supabase/seed.sql`側も対応する`toUUID(id)`で再生成すること。
> 併せて、PostgRESTの1クエリ最大1000行制限により1日分のスロットが1000件を超える場合にデータが欠落する問題（`src/lib/supabase/api.ts`）も`range`によるページングで解消した。

---

## 4. スタジオ別データ仕様・連携方針

### 4.1 サウンドスタジオノア (NOAH)
- **予約形態**: 完全Web予約システム（公式予約システム）。
- **認証仕様と「ログイン要否分離」アーキテクチャ**:
  公式予約システム（Chartカレンダー）の仕様解析により、部屋ごとに `chart_login_required_flg`（ログイン要否属性）が存在することを解明。SoundSpotでは部屋の属性に応じてクローリング挙動を2系統に完全分離。
  1. **🟢 ログイン不要スタジオ（全116部屋中 73部屋 / 約63%）**:
     - 対象: Ast系、Sst系、Gst系、Booth系、Rec系など（個人練習・小型ブース中心）。
     - 通信方式: **Cookieヘッダーなし（完全ゲストアクセス）**。
     - 特性: セッション失効という概念がなく、GitHub Actions（クラウド）やローカルで**100%永久自律稼働**。
  2. **🔒 ログイン必須スタジオ（全116部屋中 43部屋 / 約37%）**:
     - 対象: Bst系、Cst系、Est系、CSstなど（11〜28帖のバンド主力スタジオ）。
     - 通信方式: 会員ID/PW由来の有効セッション（Cookie: `PHPSESSID`）を付与してリクエスト。
- **データ破壊防止（フェイルセーフ保護）ロジック**:
  - ノアAPIはセッション切れ時にHTTP 401ではなく「HTTP 200かつ空オブジェクト `{}`」を返す仕様。
  - クローラーは空データや0スロットを検知した場合、既存の `src/data/noah-tokyo-real.json` から前回の正常スロットデータを自動サルベージし、**破壊的な空配列上書きを完全防止（キャッシュ温存）**。
- **データ取得先・スケジュール走査仕様**:
  - 各店舗の部屋マスター（公式JSON API）: `/noahweb/Chart/studios?b[]=<branch_id>`
  - 日付ごとのスケジュール枠: `/noahweb/Chart/schedule?studio_id=...&searchdate=...`
  - **5週分フェッチによる境界欠落防止ロジック**:
    ノア公式APIは「月曜始まりの1週間単位」でスケジュールを返却する仕様。基準日（何曜日であっても）から「本日〜21日先」を100%隙間なくカバーするため、基準週を含む **最大5週分（0, 7, 14, 21, 28日後）の月曜日** を走査して取得。境界日（21日目前後）のデータ欠落を完全に防止。
  - **深夜・早朝の24時以降時刻（24:30、25:30等）のISO 8601自動正規化**:
    24時間営業店舗において公式APIが返却する `24:30`, `25:30` などの営業日ベース表記を自動検知し、日付を翌日（+1日）に進め、正規の `00:30`, `01:30`（ISO 8601）に変換。ブラウザでの `Invalid Date` エラーによる表示欠落（ハイフン化）を根本排除。
- **将来の新店舗追加支援ツール (`scripts/scan-noah-branch.ts`)**:
  - 新店舗（高田馬場・池袋・恵比寿・中野など）を導入する際、`npx tsx scripts/scan-noah-branch.ts <branch_key>` を実行するだけで、公式APIから部屋一覧、帖数、開始オフセット（00分/30分）、`loginRequired`、実料金（`regular_price`/`person_price`）を瞬時に自動判定・取得可能。
- **部屋マスターの単一化 (`src/config/noah-master.ts`) と実データへの全面刷新（2026-09-14）**:
  - 従来、部屋マスター（`studioId`・畳数・開始オフセット・ログイン要否）は `scripts/lib/noah-fetcher.ts`（クロール用）と `src/lib/noah-tokyo-converter.ts`（表示用）の2ファイルに手動で複製されており、IDがズレると該当部屋のスロットが静かに空になるリスクがあった。両者が読み込む単一の `src/config/noah-master.ts` に統合。
  - 統合と同時に、7店舗・101部屋すべてのデータを公式予約API（`/noahweb/Chart/studios?b[]=<branch_id>`、`scan-noah-branch.ts`と同じ機構）および各店舗公式サイトの `/<branch>/gear/` ページから再取得し、**畳数・開始オフセット・実料金（土日祝/平日昼/個人練習）・実機材（ギターアンプ/ベースアンプ/ドラムセット）を全面的に実データへ置き換えた**。
  - 刷新前の部屋マスターは畳数・開始オフセットが大幅に誤っていたことが判明している（例: 渋谷本店Cst相当の部屋は「10帖」と記載されていたが実際は「27帖」、A1st等は「:00開始」と記載されていたが実際は「:30開始」）。原因は特定できていないが、初期データ投入時の誤りである可能性が高い。料金・機材についても、秋葉原店以外の6店舗はこれまで畳数から自動計算した近似値・汎用機材名で代用しており、実データではなかった。
  - 部屋ごとの詳細な畳数・オフセット・ログイン要否・実料金・実機材は `src/config/noah-master.ts` 本体を参照のこと（このドキュメントとの二重管理を避けるため、部屋単位の値はここには転記しない）。
  - ノア秋葉原店は本セクション（NOAH専用クロール）に完全統合済み。以前存在した「`crawlAkihabaraStudios()`側で秋葉原店のノアデータだけ個別に扱う」処理（ライブ再取得・キャッシュ読み出しのいずれも）は廃止し、`akihabara-real.json` は BOT/GOODMAN/音楽館の3スタジオのみを対象とする。これにより、同一巡回サイクル内でノアサーバーへ秋葉原店分だけ二重アクセスする問題も解消された。

### 4.2 スタジオペンタ (PENTA)
- **店舗構成と実態調査結果**:
  - ペンタ公式全11店舗の徹底巡回調査の結果、**空き状況ボード（Web）を公開しているのは「新宿店」ただ1店舗のみ**（他店舗は一切なし）。
  - 初期プロトタイプ開発時の残骸データであった架空の「新宿南口店」（公式404）は完全削除。
  - 現在の対象店舗：**渋谷3店舗（シティサイド・ジュークハウス・ムーンサイド）＋新宿店（計4店舗）**。
- **新宿店の「土日祝リアルタイム空き状況」ハイブリッド連携仕様**:
  - **予約形態**: 予約自体は全店共通で電話・店頭受付のみ。ただし**新宿店のみスタッフの手入力による「土日祝日限定の空き室ボード（Netlify埋め込み）」が稼働**。
  - **バックエンドAPI**: ペンタ新宿店空き室ボードのバックエンドである **Supabase Edge Functions の KV-API**（`https://haphgzntwgrpecdkqdxj.supabase.co/functions/v1/kv-api`）と直接連携。
  - **データ取得モジュール**: `scripts/lib/penta-fetcher.ts`
    - 部屋一覧（全19部屋）: `key=shinjuku:availability-rooms&shared=true` から取得した公式ID（`r1`〜`r12`, `room-1788...`）および部屋名（`101st L`〜`504st L`）をマッピング。
    - ブロック取得: `key=shinjuku:availability-blocks:YYYY-MM-DD&shared=true` より、スタッフが登録した使用中ブロック（`startTick`〜`endTick`、朝6時基準・30分刻み）を取得。
    - **土日祝日・平日ハイブリッド判定**: 日本の祝日（振替休日含む、〜2099年対応計算式）および土日を自動判定。土日祝日のみ30分刻み（10:00〜24:00）の `AVAILABLE` / `BOOKED` スロットを生成し、平日は電話案内のためスロット空（TELバッジ表示）として整合。
  - **保存先**: `src/data/penta-shinjuku-real.json` へ保存し、Supabase `availability_slots` へ直接同期。
- **渋谷3店舗（シティサイド・ジュークハウス・ムーンサイド）の仕様**:
  - Web空き室ボードは存在せず、**完全電話受付（`slots: []`）**。
  - 公式HPより部屋構成（全20室）、帖数（7帖〜16帖）、常設機材（JC-120, Marshall, Ampeg, Hartke, Pearl/TAMA）、料金（通常/昼割/個人練習）を網羅。
  - タイムライン上はワンタップ電話発信（`tel:03-3462-281x`）バッジを表示。

### 4.3 ベースオントップ 秋葉原昭和通り口店 (BASS ON TOP)
- **予約形態**: 公開Webカレンダー（スタジオル / shop id: `705`）。
- **認証仕様**: スケジュール・空き枠閲覧は**完全ログイン不要**。
- **データ取得先**:
  - FullCalendar / スタジオル API エンドポイント: `https://studi-ol.com/get_schedule_shop`
  - パラメータ: `shop_id=705&start=YYYY-MM-DD 00:00:00&end=YYYY-MM-DD 23:59:59`
- **予約可能・確認可能期間**:
  - **きっかり3ヶ月先（当日から91日後）まで**（例: 9月5日時点であれば **12月5日まで**）。
  - 12月6日以降（92日目以降）はイベントデータ件数が0件（未開放）となり、システム上毎日日付が変わるタイミングで1日ずつ先の日程が解禁される仕様。
- **店舗特性（全8部屋）**:
  - **00分開始部屋**: 1st (17帖), 2st (9帖), 3st (9帖), 4st (15帖), Piano (3帖)
  - **30分開始部屋**: 5st (15帖), 6st (9帖), 7st (7帖)
  - リアルタイムにバンド予約済み（`pBand`）および個人練習予約（`pInd`）のステータスを取得可能。

### 4.4 STUDIO GOODMAN AKIBA (スタジオ グッドマン)
- **予約形態**: 公開Webカレンダー（Reserve1.jp / `lc=dlcacvaol`）。
- **認証仕様**: ゲストモードで**完全ログイン不要**。
- **データ取得先**: 日別スケジュールテーブル `visitor/index.php`（文字コード: EUC-JP）。
- **店舗特性（全10部屋）**:
  - 201st〜205st, 301st〜305st の全10部屋。
  - 30分単位のHTMLテーブルで、チェックボックス（`<input type="checkbox">`）が存在するマスが予約可能枠。
  - 機材スペック: Marshall, JC-120, Ampeg SVT, Pearl/Canopus ドラム完備。

#### 4.5 スタジオ音楽館 (アキバ店・新宿西口店)
- **予約形態**: 公開PHP予約システム（ajg.jp ASP）。
- **認証仕様**: `ReservationTop.php?id=<store_id>` より動的セッションコード（`sescode`）を自動発行。完全ログイン不要で閲覧可能。
  - アキバ店: ID `Twb03vvjqn2fba1`
  - 新宿西口店: ID `Fuoxajj8krt105m`
- **データ取得技術**: 汎用スタジオ確定判定パーサー（`scripts/lib/ongakukan-fetcher.ts`）。
  - `ReservationDate.php` の店舗全体カレンダーから空き候補コマを抽出し、個別スタジオ選択画面 `ReservationStaff.php?sescode=...&date=YYYYMMDD&time=HHMM` を並行照会。
  - 画面内の予約リンク（`ReservationEnq.php?staffid=X`）に当該部屋が含まれる場合のみ `AVAILABLE`、含まれない場合は `BOOKED` として各部屋固有の空き状況を厳密確定。
  - 向こう21日間（アキバ店: 7部屋×504枠 = 3,528スロット、新宿西口店: 7部屋×504枠 = 3,528スロット）のリアルデータを完全同期。
- **店舗特性**:
  - **アキバ店 (全7部屋・6帖〜50帖)**: A〜G st（6帖〜15帖）およびMusic Inn（50帖）。毎時00分開始。電気街口徒歩3分。
  - **新宿西口店 (全7部屋・9帖〜22帖)**: 新宿駅西口徒歩2分、西新宿7-15-4 YSビルB2F。毎時00分開始。
    - Rhythm (9帖 / staffId 1), Harmony (10帖 / staffId 2), Tone (11帖 / staffId 3), Kick (13帖 / staffId 4), Opus (17帖 / staffId 5), Beat (20帖 / staffId 6), Multiplex (22帖 / staffId 7)。
    - ※48畳Attackは大型イベント専用のため窓口/電話受付。
    - 個人練習は前日21:00よりWeb/電話受付。

### 4.6 サウンドスタジオノア 都内全7店舗（SOUND STUDIO NOAH）
- **対象店舗**:
  - **渋谷本店** (14部屋・`shibuya`): Sst(18帖), A1st(15帖), A2st(13帖), B1st〜B4st, Cst, G1st〜G3st, Booth1/2, Rec
  - **渋谷1号店** (12部屋・`shibuya1`): A1st(15帖), A2st(13帖), A3st, A5st, B1st〜B3st, B5st, E1st, E2st, VoBooth, Rec
  - **渋谷2号店** (14部屋・`shibuya2`): Sst(17帖), A1st〜A3st, G1st〜G3st, B1st〜B3st, E1st, E2st, Cst, VoBooth
  - **渋谷3号店** (15部屋・`shibuya3`): A1st〜A4st, Cst, Est, DJ1st〜DJ3st, Booth1〜4, RecBooth, RecStudio
  - **新宿店** (21部屋・`shinjuku`): S1st(22帖), S2st(18帖), S3st(16帖), A1st〜A3st, A5st〜A7st, G1st〜G3st, B1st〜B3st, E1st〜E3st, CSst(25帖), Fst(13帖), Rec
  - **秋葉原店** (14部屋・`akihabara`): A1st〜A3st, B1st, B2st, Cst+Sub(28帖), E1st, E2st, G1st, G2st, GSst, Booth1/2, RecBooth
  - **御茶ノ水店** (11部屋・`ochanomizu`): Booth(4帖), A1st(8.5帖), A2st(8.5帖), Cst(18帖), Gst(12帖), B1st(13帖), A3st(8.5帖), A5st(9帖), Est(15帖), B2st(14帖), B3st(14帖)
- **予約形態**: 独自Web予約システム（会員ログインセッション利用）。
- **認証仕様 ＆ 部屋別ログイン要否分離**:
  - ノアのスタジオ構造上、全101部屋中 **約65%（66部屋）は完全ログイン不要（ゲストアクセス可能）** であり、残る **約35%（35部屋）がログイン必須** となっている。
  - **ログイン不要部屋**: セッションCookieを一切付与せずゲストアクセスで確実に取得（Cookie期限切れによる影響を完全遮断）。
  - **ログイン必須部屋**: `scripts/lib/noah-login.ts` により、純粋Node fetchで `SysMembers/login/true` からCSRFトークンを取得し、環境変数（`NOAH_LOGIN_ID`, `NOAH_PASSWORD`）で自動再ログイン・Cookie更新を実施。
  - **フェイルセーフ保護**: 万が一認証エラーやセッション失効が発生しても、直前の既存キャッシュスロットを保持・保護し、空き枠消失事故を100%防止。
- **データ取得技術**: スケジュールAPI一括取得モジュール（`scripts/lib/noah-fetcher.ts`）。
  - 各店舗の全部屋固有ID（`studio_id`）に対し、純粋なNode `fetch()` で週ごとの月曜付で3週間分（向こう21日間・計101部屋）を一括取得。
  - スロットの `is_booked`（`BOOKED`）および `is_bookable`（`AVAILABLE`）を直接判定し、`src/data/noah-tokyo-real.json` へアトミック保存。また Supabase の `availability_slots` テーブルへ直接Upsert同期。

### 4.7 ゲートウェイスタジオ 渋谷道玄坂店 (GATEWAY STUDIO SHIBUYA)
- **予約形態**: 公開Webカレンダー（Reserve1.jp / `lc=tlsccmeco&mn=8`）。
- **認証仕様**: ゲストモードで**完全ログイン不要**。
- **データ取得先**: 日別スケジュールテーブル（`member_select.php`、`SubmitFormD(dateStr)` フォーム送信による日別テーブルスクレイピング）。
- **店舗特性（全12部屋）**:
  - **3F**: 1st (15帖 :00), 2st (13帖 :00), 3st (10帖 :00), 4st (8帖 :00), 5st (8帖 Vo/Rec/Key :00), 6st (9帖 :30), 7st (12帖+専用ミーティングブース :30)
  - **4F**: 8st (9帖 :30), 9st (9帖 :30), 10st (28帖 ゲネプロ特大 :00)
  - **5F**: 11st (10帖 :30), 12st (18帖 :00)

### 4.8 STUDIO NODE 新宿店 (STUDIO NODE SHINJUKU)
- **予約形態**: 公開PHP予約ASP（`studio-node.jp` 自社設置システム / `lc=llcvcamtc&mn=1&gr=3`）。
- **認証仕様**: ゲストモードで**完全ログイン不要**。
- **データ取得技術**: 純粋Node fetchパーサー（`scripts/lib/node-fetcher.ts`）。
  - `VisitorLogin.php` よりセッションCookieを取得後、`member_select.php` に対して `day_btn=YYYY-MM-DD` をPOST送信し、日別テーブル（EUC-JP）を高速取得。
  - 00分開始室（`room_base_stt`）および30分開始室（`room_start_stt`）の全7部屋の空きチェックボックス（valueプレフィックス `01`〜`07`、コマ番号 `01`〜`14`）を完全パース。
  - 向こう21日間分の全スロット（全7部屋 × 14枠 × 21日 = 計2,058枠）を高精度に判定し、`src/data/node-shinjuku-real.json` へアトミック保存。

### 4.9 秋葉原・渋谷・新宿・高田馬場・池袋 全28店舗・完全網羅ネットワーク
- **渋谷エリア (8店舗 / 計87部屋)**:
  - ノア渋谷本店 (14室), ノア渋谷1号店 (12室), ノア渋谷2号店 (14室), ノア渋谷3号店 (15室)
  - ゲートウェイスタジオ 渋谷道玄坂店 (12室)
  - スタジオペンタ 渋谷シティサイド店 (7室 / 電話予約)
  - スタジオペンタ 渋谷ジュークハウス店 (7室 / 電話予約)
  - スタジオペンタ 渋谷ムーンサイド店 (6室 / 電話予約)
- **新宿エリア (8店舗 / 計79部屋・2026-09-20に4店舗追加)**:
  - ノア新宿店 (21室 / Web自動同期)
  - STUDIO NODE 新宿店 (7室 / Web自動同期・00分/30分開始混在)
  - スタジオペンタ 新宿店 (19室 / 土日祝リアルタイム空き状況Web同期・平日電話予約)
  - スタジオ音楽館 新宿西口店 (7室 / ajg.jp Web自動同期)
  - スタジオミュージアム新宿店 (9室 / studi-ol.com自動同期・shop 637・1F00分/2F30分開始混在)
  - ヒルバレースタジオ (3室 / studi-ol.com自動同期・shop 515)
  - Sound Studio Vantage (3室 / studi-ol.com自動同期・shop 817)
  - Music man サウンドスタジオ (10室 / Reserve1自動同期・ゲスト閲覧可能インスタンス・5タイプ×2フロア、同名部屋はフロア番号プレフィックスで区別)
- **秋葉原エリア (5店舗 / 計51部屋)**:
  - ノア秋葉原店 (14室 / Web自動同期)
  - ノア御茶ノ水店 (11室 / Web自動同期)
  - ベースオントップ 秋葉原昭和通り口店 (8室 / スタジオルAPI自動同期)
  - STUDIO GOODMAN AKIBA (11室 / Reserve1自動同期・実カレンダーは30分刻み、306st追加済み)
  - スタジオ音楽館 アキバ店 (7室 / ajg.jp自動同期)
- **高田馬場エリア (5店舗 / 計53部屋・2026-09-15追加)**:
  - ノア高田馬場店 (15室 / Web自動同期・ただしNOAH全体はGitHub Actionsブロックのためローカル実行限定)
  - ゲートウェイスタジオ 高田馬場3号店 (15室 / Reserve1自動同期・00/15/30/45分開始混在)
  - スタジオ音楽館 馬場駅前店 (6室 / ajg.jp自動同期・staffid2〜7の6室のみ対応)
  - ベースオントップ 高田馬場店 (11室 / スタジオルAPI自動同期)
  - BAZOOKA STUDIO (6室 / 会員限定Web予約のため自動巡回非対応・静的リスティング)
- **池袋エリア (6店舗 / 計51部屋・2026-09-17追加)**:
  - ノア池袋店 (11室 / Web自動同期・NOAH公式API branch_id 13)
  - ベースオントップ 池袋西口店 (7室 / スタジオルAPI自動同期・shop_id 2355、旧北口店は閉店済)
  - ゲートウェイスタジオ 池袋北口店 (16室 / Reserve1自動同期・5F/6F 2フロア統合・00分/30分開始混在)
  - スタジオペンタ池袋店 (5室 / 電話予約のみ・静的リスティング)
  - スタジオペンタ池袋ハンズサイド (8室 / 電話・LINE予約のみ・静的リスティング)
  - Vivo Sound Studio (4室 / 電話・LINE予約のみ・静的リスティング、グランド/アップライトピアノ常設室あり)
- **運用ポリシー**:
  - Web同期店舗（21店舗・248部屋）: ノア9店舗（127室）、ゲートウェイ3店舗（43室）、NODE（7室）、BOT3店舗（26室）、GOODMAN（11室）、音楽館3店舗（20室）、ペンタ新宿店（19室）は純粋Node fetchによる完全クラウド自動巡回で21日間分の空きスロットを常時同期（ノアのみIPブロックのためローカル実行限定）。
  - 完全電話予約店舗（ペンタ渋谷3店舗・ペンタ池袋2店舗・Vivo Sound Studio・計37部屋）: 全室スペックマスター（帖数・定員・アンプ・料金）を完全網羅し、ワンタップ電話予約CTA（`tel:`）で安全・確実に誘導。
  - 会員限定Web予約店舗（BAZOOKA STUDIO・6部屋）: reserve1.jp（ReserveMart）採用だが空き状況閲覧に会員登録必須のため自動巡回非対応。部屋・機材スペックのみ掲載し、予約は公式サイトへ誘導。

### 4.10 新規追加候補エリアの現営業中スタジオ実態調査（高田馬場・池袋）
> **2026-09-17時点の状況**: 高田馬場・池袋の両エリアとも、以下の実態調査を踏まえ4.9節に記載の店舗として実装済み（詳細な部屋数・機材・オフセット等は実クロールにより本節の初期調査値から補正されている）。

公式Webサイトおよび予約システムの稼働状況を検証し、**現在確実に営業している実店舗**を精査（閉店・統合店舗は除外）。

#### 1. 高田馬場エリア（実装済み・5店舗）
| 店舗名 | 部屋数 | 予約システム | 特記事項・稼働検証結果 |
|---|:---:|---|---|
| **サウンドスタジオノア 高田馬場店** | 15部屋 | ノア公式API (`takadanobaba` ID:11) | 24時間営業。公式API稼働確認済。 |
| **ベースオントップ 高田馬場店** | 11部屋 | スタジオル (`studi-ol.com/shop/681`) | 24時間営業。全11部屋。内部room_id(2919〜2929)を特定し疎通確認済。 |
| **スタジオ音楽館 馬場駅前店** | 6部屋 | ajg.jp (`Pnpa4ff1gt9m22p`) | 大輝ビルB1F。公式は全8スタジオだが、通常予約フローで出現するのはstaffid2〜7の6室のみ。 |
| **ゲートウェイスタジオ 高田馬場3号店** | 15部屋 | Reserve1.jp (`mn=3&gr=4`) | 和光ビルB棟。カレンダー起点10:00、00/15/30/45分開始混在の特殊フォーマット。稼働確認済。 |
| **BAZOOKA STUDIO** | 6部屋 | Reserve1.jp/ReserveMart (`mn=3&gr=1`、会員限定) | 三優ビルB1F。空き状況閲覧に会員登録必須のため自動巡回非対応、静的リスティングのみ。 |

#### 2. 池袋エリア（現営業中 6店舗）
| 店舗名 | 部屋数 | 予約システム | 特記事項・稼働検証結果 |
|---|:---:|---|---|
| **サウンドスタジオノア 池袋店** | 14部屋 | ノア公式API (`ikebukuro` ID:13) | 東池袋。24時間営業。公式API稼働確認済。 |
| **サウンドスタジオノア 池袋ANNEX店** | 6部屋 | ノア公式API (`ikebukuro_annex` ID:112) | 南池袋。ピアノ・個人練習・セルフレコ特化。公式API稼働確認済。 |
| **ベースオントップ 新池袋西口店** | 11部屋 | スタジオル (`studi-ol.com/shop/701`) | 西口徒歩2分。全11部屋。APIエンドポイント疎通確認済。 |
| **スタジオペンタ 池袋店** | 6部屋 | 電話予約 (03-3984-0811) | 東口徒歩3分。空き状況ボードなし・電話受付のみ確認済。 |
| **スタジオペンタ 池袋ハンズサイド店** | 7部屋 | 電話予約 (03-3984-2811) | サンシャイン通り。空き状況ボードなし・電話受付のみ確認済。 |
| **ゲートウェイスタジオ 池袋北口店** | 16部屋 | Reserve1.jp (`lc=tlsccmeco&mn=3&gr=1`) | 西池袋1-43-7 福住ビル5F・6F。5F(Ast〜Gst)/6F(1st〜8st+SUBROOM)が同一カレンダーを共有。2026-09-17に実装・稼働確認済。 |

---

## 5. UI / タイムライン設計ロジック

### ① 30分開始枠の物理シフト（50%オフセット配置）
ノアなどのスタジオでは、ロビーの混雑緩和のため「00分スタートの部屋」と「30分スタートの部屋」が混在しています。これを文字ラベルだけでなく**視覚的に一目で区別**できるグリッド構造を採用しました。

```
[時間目盛]   |  10:00  |  11:00  |  12:00  |  13:00  |
------------------------------------------------------
00分開始部屋 | [ 10:00-11:00 ] | [ 11:00-12:00 ] | ...
30分開始部屋 | (空白) | [ 10:30-11:30 ] | [ 11:30-12:30 ] ...
               ↑ 半枠(30分)右に物理シフト
```

- **グリッド構成**: 06:00〜24:00（18時間）を **36分割（1時間＝2列）** のCSS Gridで描画。
- **00分開始部屋**: 06:00からそのまま2列幅（`col-span-2`）で配置。
- **30分開始部屋**: 先頭に1列分（30分相当: 06:00〜06:30）の空スペーサーを自動挿入し、以降のスロットを**時間ヘッダーのちょうど中間（50%位置）から物理的にずらして配置**。
- **バッジ配色**:
  - `青`: 通常枠（00分開始）
  - `紫`: 時差枠（:30開始）
  - `黄`: 電話受付スタジオ

### ② 個人練習ルールの取り扱い
- 個人練習は「前日21時受付開始」「1〜2名限定」「特定料金」など各社でルールが大きく異なります。
- バンド予約の空き枠と混同しないよう、料金表示には「バンド通常料金」と「個人練習料金（¥xxx/人/h）」を併記し、詳細モーダルで注意事項を案内します。

### ③ 「1スタジオ ＝ 1親カード ＋ 部屋リスト」集約表示設計（無駄な重複情報の徹底排除）
- **課題**: 以前の「1部屋＝1カード」方式では、同じスタジオ（音楽館やGOODMAN等）のヘッダーやロゴ、住所が何度も縦に並び、さらに全カードに「14:00〜15:00の空き状況」という重複した大きな帯が表示され、画面のスクロール効率と視認性を著しく損なっていた。
- **仕様**:
  - **構造**: **「1スタジオ ＝ 1親カード」** とし、その内部にスタジオ内の各部屋をスリムな行（リスト）で並べる構造に全面リファクタリング。
  - **親カードヘッダー**:
    - スタジオ名、チェーン名、最寄り駅、営業時間（24h等）、価格帯レンジ（¥2,200〜¥10,640/h）、該当時間帯の「空き部屋数（例: 4部屋 空きあり）」を1回だけ表示。
  - **部屋行（スリムリスト）**:
    - 各行に「部屋名」「帖数」「開始分（:00/:30）」「主要アンプ（JC/Marshall）」「料金（¥/h）」「空き/満室アイコン」「詳細開く矢印」を横1行でコンパクトに配置。
  - **重複文言の完全排除**:
    - ユーザーが自ら検索条件で指定している時間帯（例: 14:00〜15:00）を各カードに文言で再掲するのを廃止し、スリムな `[✓ 空き]` / `[✕ 満室]` バッジに集約。
  - **カードフッター**:
    - Web予約スタジオは「公式WEB予約サイトを開く ↗」、電話受付スタジオは「お電話で空き確認・予約 (tel:)」ボタンをカード最下部に1つ配置。
- **効果**:
  - 秋葉原の全36部屋がわずか数枚のスタジオカードに美しく整理され、1画面内で各スタジオの空き部屋・帖数・料金が即座に比較可能となった。

### ④ 満室部屋の表示・非表示トグル設計（空検索時のノイズ除去）
- **課題**: 混雑する日時（土日の昼〜夜など）では大半の部屋が予約済み（満室）となり、予約できないカードをスクロールし続ける必要があり、探している空き部屋を見落としやすい。
- **仕様**:
  - **デフォルト動作**: **「満室の部屋を非表示（空き枠ありのみ表示）」を標準（ON）** とする。
  - **UIトグル**: 検索結果件数の横に `[● 満室を非表示中]` のトグルボタンを常設。ワンクリックで `[● 満室も表示中]` へ切り替え可能。
  - **判定ロジック**: ユーザーが指定した希望時間帯（例: 14:00〜16:00）に重なるスロットのうち、1つも `available` が存在しない部屋を瞬時に除外する。

### ⑤ スタジオ・部屋の自然整列ルール（公式スタジオ順・アルファベット・階数優先）
- **方針**: 00分開始・30分開始での人工的なグループ分割（00分部屋を全室並べた後に30分部屋を並べる等）は行わない。30分開始はタイムラインの視覚的シフト（50%オフセット）で直感把握できるため、部屋順序の優先度からは外す。
- **仕様**:
  - **ソート基準**: 各スタジオ公式の部屋命名規則およびフロア構成に準拠した**自然順序（Natural Order）** を標準とする。
    1. **BASS ON TOP**: `1st, 2st, 3st, 4st, 5st, 6st, 7st, Piano`（部屋番号順）
    2. **STUDIO GOODMAN**: `201st〜205st (2F), 301st〜305st (3F)`（階数・部屋番号順）
    3. **スタジオ音楽館**: `Aスタジオ, Bスタジオ, Cスタジオ, Dスタジオ, Eスタジオ, Gスタジオ, Music Inn`（アルファベット順）
    4. **サウンドスタジオノア**: `A1st, A2st, A3st, B1st, B2st, Cst+Sub, E1st, E2st, G1st, GSst, Booth1`（部屋記号・フロア順）
  - ソートドロップダウンに「スタジオ・部屋順（公式）」を新設し、デフォルトの並び順として採用。空き枠優先や料金順ソート時も、同順位のタイブレークにこの自然順序を適用する。

### ⑥ 早朝・モーニング枠の表示切替（デフォルト9〜24時 & トグル展開）
- **背景**: 一般的なバンド練習需要の大半は午前〜深夜帯（9:00〜24:00）であり、最初から朝6:00を表示すると横幅が冗長になり視認性が下がる。一方で早朝練習・モーニングパック需要も確実に存在する。
- **仕様**:
  - **標準表示**: デフォルトは **09:00 〜 24:00**（15時間 = 30カラム）とし、画面をすっきり整理。
  - **早朝枠トグル**: 検索バーおよびタイムライン上部に `[ ] 🌅 早朝(6〜9時)` のトグルチェックボックス/ボタンを常設。
  - **展開動作**: ONに切り替えると、検索セレクトおよびタイムラインが滑らかに **06:00 〜 24:00**（18時間 = 36カラム）へ即座に展開される。

### ⑦ タイムライン指定枠の強調表示（ハイライト＆自動1時間後連動）
- **仕様**:
  - **1時間後自動設定**: 検索バーの開始時間（例: `14:00`）を選択した際、またはタイムライン上の任意のマスをクリックした際、自動的にその1時間後（`15:00`）が終了時刻としてセットされる。
  - **現在指定枠の視覚的強調**:
    - ユーザーが現在指定している時間帯（例: 14:00〜15:00）の列ヘッダーに `14:00 ◀指定` のエメラルドバッジを表示。
    - **【重要】空き枠（予約可）のみ強調表示**:
      - 該当コマが **空き枠（予約可）** の場合のみ、**鮮明な発光リング（ring-2 ring-emerald-400）・拡大（scale-[1.03]）・時刻テキスト（14:00）・パルスインジケーター** を施し、自分が狙っている枠がタイムライン上で一目で浮き上がって見えるように強調する。
      - 前後30分枠が空きの場合は、青色リング枠に時刻テキスト（14:30等）を表示。
    - **【重要】予約済み（予約不可）の扱い**:
      - 指定時間帯（完全一致・前後30分枠）であっても、**予約できないスロットには時刻テキストや枠線強調を表示せず、他の予約済みマスと「同じく×マーク（ダークグレー背景・控えめなトーン）」で完全に統一**する。
      - これにより、予約できない枠がボタンのように浮き上がって空き枠と誤認されるUI上の混乱を完全に防止する。
  - **ダイレクト時間選択**: タイムライン上の空きマスをクリックするだけで、その時間が検索バーの時間帯に瞬時に同期される。

### ⑧ エリア指定チェックボックス（複数エリア同時選択＆即時絞り込み）
- **背景**: 従来の単一セレクトボックスでは「渋谷と新宿の両方で空いているスタジオを同時に探したい」といった複数エリア横断ニーズに対応できなかった。
- **仕様**:
  - **チェックボックスボタングループ化**: 「秋葉原」「渋谷」「新宿」などの各エリアを独立したチェックボックスボタンとして配置。
  - **複数選択可能**: 「渋谷」と「新宿」など、希望する複数エリアにチェックを入れて同時検索が可能。
  - **未選択時のフォールバック**: 全てのチェックを外した（0個選択の）場合は、ゼロ件にするのではなく「全エリアを表示」として扱い、ユーザーが迷わない安心設計。
  - **クリア機能**: エリアが1つ以上選択されている場合、右上に `[クリア]` ボタンが表示され、ワンタップで全エリア表示に戻すことが可能。

### ⑨ 広さ指定チェックボックス（複数選択＆即時絞り込み）
- **背景**: 従来の「10帖以上」「12帖以上」といった単一の下限ドロップダウンでは、「10〜12帖のちょうど良い部屋を探したい」「狭すぎる部屋や広すぎて高額な部屋を除外したい」というバンドマンの実際のニーズに応えられなかった。
- **仕様**:
  - **選択肢**:
    - `〜9帖`: 個人練習・少人数（2〜3人）向け
    - `10〜12帖`: 3〜4人バンド標準
    - `13〜15帖`: 4〜5人バンド・ゆったり
    - `16帖〜`: 大人数・ビッグバンド・ゲネプロ向け
  - **複数選択可能**: 独立したチェックボックスボタングループとし、例えば「10〜12帖」と「13〜15帖」を同時選択して中〜大型部屋のみを一覧化できる。
  - **未選択時の動作**: チェックが0個の場合は「全広さを表示」として動作し、1つ以上選択された時点で該当範囲の部屋のみを即座にフィルタリングする。
  - **クリア機能**: 1つ以上選択されている場合、ラベル横に `[クリア]` ボタンが表示され、ワンタップで全選択解除が可能。

### ⑩ 24時間・長時間営業（深夜・早朝）スタジオの視認性設計
- **背景**: 深夜練習や早朝練習を行いたいユーザーに対し、どの店舗がオールナイトや早朝に対応しているかを迷わせない。
- **仕様**:
  - **カード＆モーダル**:
    - `🌙 24時間営業`（ノアなど）: シアン/エメラルドのネオン調バッジ
    - `✨ 翌朝5時まで (土日朝9時~)`（ベースオントップ）: インディゴバッジ
    - `✨ 予約時24h対応`（音楽館）: インディゴバッジ
    - モーダル内に営業時間サマリーを明記。
  - **タイムライン**:
    - 各行のスタジオ名横に `[24h]` や `[深夜早朝]` のミニバッジを配置し、なぜそのスタジオだけ特定時間帯に枠があるのかを直感的に把握可能。
  - **こだわり条件フィルター**:
    - 機材フィルター群に `✨ 24時間・長時間営業のみ` の絞り込みボタンを追加。ワンクリックで長時間営業スタジオのみを抽出可能。

### ⑨ ツインペダル条件の撤廃（UIのノイズ削減）
- **背景**: 国内のリハーサルスタジオ（ノア、ベースオントップ、スタジオ音楽館、STUDIO GOODMAN等）では、常設ドラムセットへの自前ツインペダルの持ち込み・使用は原則として制限されず、標準で許可されている。
- **仕様変更**:
  - 検索フィルターバーから「ツインペダル使用可能」トグルを完全撤廃。
  - スタジオカードのタグ表示、および部屋詳細モーダルのドラムセット欄から「ツインペダル可否」バッジを削除し、JC-120/Marshall/アンプ型番/レコーディングなど真に差別化される機材情報に絞り込んで表示のノイズを低減。

### ⑩ スマートフォン（モバイル）表示最適化方針（Mobile-First Optimization）
- **背景**: バンドマンがスタジオを探すシチュエーションの多くは外出先や移動中のスマートフォン利用（片手操作・親指タップ）であるため、狭いビューポート（375px〜430px）での視認性と操作性を最優先する。
- **仕様**:
  - **予約種別切り替えタブ**: スマホ画面では `grid-cols-2` による2列等分割レイアウトを採用し、片手でも親指が届きやすく押し間違いのないタップ領域を確保。
  - **機材・こだわりフィルター**: タグ群を折り返して縦長になりすぎないよう、横スクロール（`overflow-x-auto`）＋コンパクトチップ（`py-1.5 px-3 text-xs`）を採用。
  - **スタジオ親カード（集約リスト）**:
    - 店舗名ヘッダーのフォントサイズをスマホ向けにレスポンシブ調整（`text-base sm:text-lg`）。
    - 部屋行（Room Row）は左側に「部屋名・帖数・開始分」、右側に「料金・空きバッジ」をコンパクトに配し、狭小画面でも不自然な改行やはみ出しを防止。
    - 最下部のCTAボタン（「公式WEB予約を開く ↗」/「電話で空き確認・予約」）は、スマホでの押しやすさを重視し **高さ44px（`h-11`）以上の押しやすいタッチターゲット** を確保。
  - **タイムラインのレスポンシブ固定列幅 & 左端完全密着 (`sticky left-0`)**:
    - 1列目（スタジオ名・部屋名固定列）の幅を、PC時は `220px`、スマホ時（`sm` 未満）は **`135px`** に最適化。
    - スマホ幅（375px〜390px）でも、右側のスロット表示・スライド可能領域を約240px〜255px確保し、横スクロール操作を格段に快適化。
    - スクロールコンテナ（`overflow-x-auto`）の左右パディングを排除（`px-0`）し、固定列が左端 0px に1ミリの隙間もなく完全に密着。パディングの隙間からスロットがチラ見えする現象を完全解消。
    - スタジオ見出し行の第1列（スタジオ名）を `sticky left-0 z-30 bg-slate-800 border-r`、各部屋行の第1列（部屋名）を `sticky left-0 z-20 bg-slate-900 border-r` とし、完全不透明なソリッドカラーと適切な z-index 設計により、横スクロール時に右側のスロットや補足情報（駅名・公式リンク）が固定セルの裏に綺麗に潜り込む構造を確立。スタジオ名は最大幅（135px/220px）で自動 `truncate` 処理。
  - **対応スタジオ一覧モーダルのスマホ固定解除・スリム化**:
    - モーダルヘッダー内のサマリーカード（対応エリア、店舗数、部屋数等の4枚のカード）を固定ヘッダーから除外し、スクロール可能エリアの先頭へ移動。
    - スマホ表示時の固定ヘッダー高さを約70px〜90pxに圧縮し、一覧の可視領域を画面の約80%まで大幅拡大。

### ⑪ 対応スタジオ・部屋・常設機材一覧モーダル（SupportedStudiosModal）
- **背景**: ユーザー（バンドマン）にとって「このアプリが自分の使いたいスタジオに対応しているか」「どこにどんな部屋や機材（Marshall, JC-120, ドラム等）があるか」は最重要関心事であるため、Topヘッダーおよびヒーローセクションからワンクリックで全対応店舗と全部屋・機材スペックを把握できる一覧導線を設置。
- **仕様**:
  - **Topヘッダー稼働状況トリガー**:
    - ヘッダー右上に配置されている `稼働状況: 都内 16店舗 / 169部屋 稼働中` バッジをインタラクティブなボタン（`cursor-pointer`、ホバー発光、`[一覧を見る ↗]` ガイド）に刷新。
    - Topページのヒーローセクション内にも「`⚡ 対応スタジオ一覧 (16店舗) を見る ↗`」のショートカットボタンを常設。
  - **サマリー統計（スクロール領域内配置）**:
    - 3大エリア（渋谷 8店舗 / 新宿 4店舗 / 秋葉原 4店舗）
    - 計16スタジオ・169部屋の規模感
    - リアルタイムWeb同期 10店舗 / 電話予約 6店舗
  - **絞り込み・機材検索機能**:
    - エリアタブ切替（すべて 16 / 渋谷 8 / 新宿 4 / 秋葉原 4）
    - インクリメンタルキーワード検索（店舗名、最寄り駅に加え、「Marshall」「JC-120」「Canopus」などの**常設機材名でのスタジオ検索**にも完全対応）
  - **常設部屋・機材一覧のアコーディオン展開（全16店舗・全室スペック網羅）**:
    - 各スタジオカード内に **「常設部屋・機材一覧（全◯部屋） ▼」** のアコーディオン開閉ボタンを追加（上部の一括開閉ボタンで全店舗一括展開も可能）。
    - 展開すると、その店舗の全室が一覧展開され、「部屋名」「帖数」「定員」「料金（通常/個人）」「ギターアンプ（Marshall/JC-120等）」「ベースアンプ（Ampeg等）」「ドラム型番」「キーボード」などのスペックをカード形式で一目で把握可能。
    - 各部屋カードをクリックすると、既存の `RoomDetailModal`（部屋詳細モーダル）が開き、さらに詳細な設備・写真を確認可能。
  - **店舗カード情報**:
    - 店舗名、チェーンブランド名、最寄り駅（徒歩分数）、全部屋数、営業時間（24h/10〜24時）
    - 連携ステータスバッジ（`⚡ リアルタイムWeb同期` / `📞 電話予約`）
    - 店舗特徴タグ（例:「24時間営業」「個人練習Web受付」「グランドピアノ室」）
    - 公式サイトリンク（`↗ 公式HP`）および電話予約スタジオのワンタップ発信（`tel:`）CTA
  - **フッターポリシー明記**:
    - 定時自動巡回（深夜完全睡眠・Jitter安全制御）による公式連携方針を明記し、ユーザーへ安心感を提供。


### ⑫ タイムライン表示の重複単語・無駄の徹底排除（スタジオ別グルーピング＆軽量セル表示）
- **課題**: 以前のタイムラインでは、全36部屋の行ごとに「STUDIO GOODMAN AKIBA」「スタジオ音楽館 アキバ店」などの同一店舗名が延々と反復され、さらに各マス（数百コマ）に「9:00」「10:00」という時間テキストがぎっしり埋め込まれていたため、肝心の部屋名や空き状況が視覚的ノイズに埋もれていた。
- **仕様変更**:
  - **スタジオ別グループ化（スタジオ見出し行の新設）**:
    - カード表示と同様、スタジオごとに見出しヘッダー（店舗名・最寄り駅・24h営業バッジ・公式予約リンク）を1回だけ表示。
    - 各部屋の行からは冗長なスタジオ名を省き、`部屋名（帖数）` と `料金` のみを左側にスッキリ配置。
  - **マス（セル）内テキストの削ぎ落とし**:
    - 上部に時間軸ヘッダーが常設されているため、セル内の「9:00」「10:00」などの数字テキストを全廃。
    - 空き枠は清潔なエメラルド色＋シンプルな `○`、予約済みはダークスレートの `×` で表示し、未取得の破線 `—` と明確に区別してスケジュール表としての直感性を向上。
    - **現在ユーザーが指定している時間枠（例: 14:00〜15:00）のみ**、セル内に時刻を明記し発光リングで強調。
  - **「00分開始」バッジの撤廃**:
    - 00分開始は業界標準であり、マス配置自体が00分開始基準のため文字バッジを削除。30分開始部屋のみ控えめな `:30` タグを付与。

### ⑬ 前後30分枠（±30分）の柔軟マッチング仕様（最大候補数の提案）
- **背景**: 都内のリハーサルスタジオは「毎時00分開始スタジオ（GOODMAN、音楽館等）」と「毎時30分開始スタジオ（ノア、ベースオントップの一部部屋等）」が混在している。ユーザーが例えば「11:00〜12:00」で厳密検索すると、30分開始スタジオの「10:30〜11:30」や「11:30〜12:30」といった空き枠がすべて満室扱いとなり、予約可能な有力候補を見落とす原因となっていた。バンドメンバー間では30分前後の時間調整は容易に行われるため、前後30分の枠も含めて広く空き枠を提案する。
- **仕様**:
  - **判定優先順位**:
    1. **完全一致（Exact）**: 指定時間（例: 11:00〜12:00）に空きがある枠。
    2. **前30分（Early: -30分）**: 30分前（例: 10:30〜11:30）に空きがある枠。
    3. **後30分（Late: +30分）**: 30分後（例: 11:30〜12:30）に空きがある枠。
  - **UI強調表示ルール（カード＆タイムライン共通）**:
    - **完全一致（Exact Match）**: **緑強調表示（Emerald Highlight）**
      - 指定開始時刻と完全に一致する枠（例: 11:00開始指定時の11:00枠、12:30開始指定時の12:30枠）。
      - **スタジオカード**: `[✓ 空き]`（エメラルド背景 `bg-emerald-950`、グリーン枠線 `border-emerald-600`、グリーン文字 `text-emerald-300`）。
      - **タイムライン**: エメラルド発光リング強調（`ring-2 ring-emerald-400 border border-emerald-300 bg-emerald-500/35`、エメラルド点滅ドット、開始時刻明記）。
    - **前後30分ズレ（±30min Shifted Match）**: **青強調表示（Blue Highlight）**
      - 指定開始時刻から前後に30分ズレた枠（メンバー間での時間調整が容易な候補枠）。
      - **具体例1（11:00開始で検索した場合）**:
        - 11:00開始枠 ➔ **緑強調（完全一致）**
        - 10:30開始枠 ➔ **青強調（前30分ズレ）**
        - 11:30開始枠 ➔ **青強調（後30分ズレ）**
      - **具体例2（12:30開始で検索した場合）**:
        - 12:30開始枠 ➔ **緑強調（完全一致）**
        - 12:00開始枠 ➔ **青強調（前30分ズレ）**
        - 13:00開始枠 ➔ **青強調（後30分ズレ）**
      - **スタジオカード**: `[✓ 10:30~ 空き]` / `[✓ 11:30~ 空き]` / 両方空き時は `[✓ 10:30/11:30~ 空き]`（ブルー背景 `bg-blue-950`、ブルー枠線 `border-blue-500`、ブルー文字 `text-blue-300`）。
      - **タイムライン**: ブルー発光リング強調（`ring-2 ring-blue-400 border border-blue-300 bg-blue-900/85 text-blue-100`、ブルー点滅ドット、開始時刻明記）。
      - **タイムライン凡例**: `完全一致 ({targetStartTime}〜{targetEndTime})`（緑ドット）と `前後30分枠 (調整候補)`（青ドット）を併記し、初見のユーザーでも色の意味が一目で理解できる設計。
  - **検索バー設定**:
    - 希望時間帯の直下に `[✓] 前後30分の枠も含める (±30分)` のチェックボックスを常設（**デフォルトON**）。厳密なピンポイント検索を行いたい場合はOFFに切り替え可能。
  - **満室除外フィルター（`hideFullyBooked`）**:
    - 指定時間または±30分枠に空きがある部屋は「空きあり」とみなし、除外されずにカード・タイムラインに表示。
    - **ソート（空き枠優先時）**:
      - 完全一致枠を持つ部屋（スコア4） > 前後30分枠を持つ部屋（スコア3） > 電話予約スタジオ（スコア2） > 未取得部屋（スコア1） > 満室部屋（スコア0）の順に自動ソート。

### ⑭ A8.net画像バナー広告設計（サイドバー2枠＋下部固定バー1本、2026-09-20一本化）
- **背景・変遷**:
  - 当初はエバーグリーンなテキスト/ネイティブPRカード（`NativeAdCard`/`NativeAdBanner`）をフィード内・タイムライン下部に個別配置していたが、楽器店・音楽教室・グッズ制作・カーシェア等13社超のA8.net提携が進んだことで広告主のバリエーションが増え、実素材（バナー画像）ベースの表示に一本化した。
  - さらに、PC/スマホそれぞれでフィード内広告・タイムライン下部広告・横長バナーが別々に存在すると広告面が乱立し視認性が下がるため、**PCは右サイドバー2枠、スマホは画面下部固定の横長バナー1本、と画面幅ごとに広告面を1箇所ずつに一本化**し、フィード内・タイムライン内の個別広告枠（`NativeAdCard`/`NativeAdBanner`/旧`MobileHorizontalAdBanner`）は廃止した。
- **広告設定の一元管理**: `src/config/banner-ads.ts` の `BANNER_ADS` 配列に全広告主（画像URL・クリックURL・トラッキングピクセル・サイズ）を集約。
  - `SIDEBAR_ROTATION_ADS`（300×250のみ）: PCサイドバー2枠（`SidebarBannerAd` × `startIndex=0,1`）で使用。それぞれ独立した8秒間隔の自動ローテーションを持ち、開始位置をずらすことで2枠が同時に同じ広告主にならないようにしている。
  - `HORIZONTAL_BANNER_ADS`（幅/高さ比 >= 3の横長素材、320×50・468×60等）: スマホの画面下部固定バー（`FixedBottomAdBanner`）で使用。
- **UI / UX 配置**:
  - **サイドバー（PC専用、lg以上）**: 検索結果カード表示の右側に `sticky` で追従する縦2枠。スマホでは非表示。
  - **画面下部固定バー（スマホ専用、lg未満）**: `src/app/layout.tsx` にマウントし、`lg:hidden` でPCでは非表示。全ビュー（カード/リスト・タイムライン）で常時表示し、8秒おきに広告主を自動切替。`<main>`・`<footer>`にスマホのみ `pb-20` の余白を確保し、他のUI（予約CTAボタン等）を隠さないようにしている。
  - **バナー自体のデザイン**: `HorizontalBannerAd`はヘッダー・フッターのテキスト行を持たず、画像左上に小さく「PR」ラベルを重ねるミニマルな見た目（常時表示でも圧迫感を出さないため）。`SidebarBannerAd`は従来通りPRバッジ・広告主名・「広告」表記を持つカード形式。
- **インプレッション追跡（トラッキングピクセル）**:
  - A8.netの規約に基づき、各広告に `1x1` の不可視トラッキング画像（`<img src={trackingPixel} ... />`）を埋め込み、表示回数（インプレッション）を記録。
- **SEO / ガイドライン準拠**:
  - 全PRリンクに `rel="noopener noreferrer sponsored"` を付与し、検索エンジンの広告リンクポリシーに完全準拠。

### ⑮ カレンダー利用日選択の3週間制限（クローリング同期範囲との整合）
- **背景**: システムがバックグラウンド巡回（GitHub Actions）で実際に取得・保持しているリアルタイム空き枠データは「本日〜3週間先（21日間）」である。カレンダーでそれ以降の日付が自由に選択できてしまうと、データが存在しない未来の日付に対してすべて「未取得」または「満室」と表示され、ユーザーに誤認を与えるUX上の齟齬が発生する。
- **仕様**:
  - **検索バーの日付選択制約**:
    - `<input type="date">` に `min={本日}` および `max={本日 + 21日}` をHTML標準属性として設定。
    - 入力欄直下に `※リアルタイム空き枠同期中: 本日〜3週間先まで` のガイド文を明記。
  - **フロントエンド・状態管理のガード（クランプ処理）**:
    - URLパラメータや直接入力等で範囲外の日付が渡された場合、`HomePage` の状態変更ハンドラ（`handleFiltersChange`）で自動的に `[本日, 本日 + 21日]` の範囲内にクランプ（補正）する。
  - **クイック日付ボタンの適合**:
    - 「今日」「明日」「今週末（土）」「今週末（日）」の各ショートカットボタンは常に7日以内のため、本制約と完全整合。

### ⑯ 未取得（未同期）スロットの明示表示設計（満室・電話受付との3態分離）
- **背景**: 従来の実装では、クローリング未実施の日時や、APIから特定時間帯のデータが取得できていないWebスタジオのスロットについて、判定ロジックが「空き枠なし（`available: false`）」として扱い、結果として `[✕ 満室]` と赤色表示されていた。これにより、実際には空いている可能性があるにもかかわらず「埋まっている」と誤解される問題が生じていた。さらに、予約済みが `-`、未取得が `—` だと視覚的に紛らわしかったため、予約済みを明確に `×` で表現する。
- **仕様（スロットの3態分離）**:
  スタジオおよび各部屋・各時間枠の状態を以下の3つに厳密に分離・定義。
  1. **確定ステータス（データ取得済み）**:
     - `○ 空き`（エメラルド）: クローラーが空き枠を確認済み。
     - `~ 空き`（ブルー）: 前後30分枠に空きを確認済み。
     - `✕ 満室` / `× 予約済`（スレート）: クローラーが予約済みであることを確認済み。
  2. **電話受付専門スタジオ（PENTA等）**:
     - `要TEL` / `TEL`（アンバー）: Web予約非対応のため、電話で確認・予約するスタジオ。
     - スタジオカード下部CTAは「電話で予約 (`tel:`)」を優先表示。
  3. **未取得・未同期（Unfetched）**:
     - `— 未取得` / `— 未同期` / `—`（スレート破線 `border-dashed border-slate-700`、文字 `text-slate-500`）:
       - 当該日時のスロットデータがクローラーにより取得されていないWeb予約スタジオ。
       - ツールチップに「この日時の空き枠データは未取得です。公式WEB予約サイトをご確認ください。」と案内。
       - スタジオカードの全室が未取得の場合は親カードヘッダーにも `[— 未同期]` を表示（満室の赤バッジと明確に区別）。
       - タイムラインビューの該当マスも破線枠 `—` とし、予約済みの `×` と視覚的に完全分離。凡例にも「未取得」を追加。
       - スタジオカード下部CTAは電話ではなく正規の「公式WEB予約を開く ↗」を確実に提供。
  - **満室非表示トグル（`hideFullyBooked`）の挙動**:
    - 「満室を非表示」が有効な場合でも、未取得（`unfetched`）および電話受付（`phone_only`）の部屋は**除外せず表示を維持**し、ユーザーがスタジオの存在を見落とすことを防止。
  - **コンバーター内の日付スライド投影・空き枠捏造フォールバックの完全撤廃**:
    - 初期プロトタイプにおいて、秋葉原（`akiba-converter.ts`）、ゲートウェイ（`gateway-converter.ts`）、ノア（`noah-converter.ts`）で実データ範囲外の日付が指定された場合に過去スロットを日付書き換えで投影したり空き枠を自動生成していた暫定ロジックを完全撤廃。
    - 実データの日付と厳密に一致（`slot.start_time.startsWith(targetDate)`）するスロットのみを抽出し、該当日のデータが存在しない場合は確実に `slots = []`（未取得）として扱い、ユーザーへ虚偽の空き枠を見せないデータ整合性を確立。

---

## 6. エリア別スタジオ調査＆スクレイピング実現可能性マップ

### 6.1 秋葉原・神田エリア
山手線東側最大の音楽・サブカルチャー拠点。全4大スタジオを網羅済み。

| スタジオ名 | 最寄駅 | 予約方式 | カテゴリ | 空き枠取得の実現可能性 / 技術仕様 |
|---|---|---|---|---|
| **ベースオントップ 秋葉原昭和通り口店** | 秋葉原 徒歩2分 | スタジオル (studiol.jp) | **カテゴリA (公開Web)** | **【高・極めて容易】**<br>全国展開の「スタジオル」システムを採用。カレンダーは一般公開されており、ログイン不要で空き枠スクレイピング/API取得が可能。 |
| **サウンドスタジオノア 秋葉原店** | 末広町 徒歩1分 / 秋葉原 徒歩6分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【済・技術検証完了】**<br>共通WebAPI。認証セッション（Cookie）により同日全スロットを一括取得可能。 |
| **STUDIO GOODMAN AKIBA** | 秋葉原 徒歩5分 | Reserve1 (ASP) | **カテゴリA (公開Web)** | **【高・取得極めて容易】**<br>「ゲスト様」として完全ログイン不要で日別空き状況テーブルが一般公開。30分単位のHTMLテーブル構造で、チェックボックス（選択可能＝空き）の有無や記号（×＝予約済、🚫＝不可）により高精度かつ簡単にパース可能。 |
| **スタジオ音楽館 アキバ店** | 秋葉原 徒歩3分 | ajg.jp (PHP予約システム) | **カテゴリA- (公開Web)** | **【高・取得可能】**<br>ログイン不要で1時間ごとの空き状況（`ReservationDate.php`）を一般公開。セッションコード（`sescode`）を自動発行してアクセスすることで、全部屋の空き枠をスクレイピング可能。 |
| **リンキィディンクスタジオ 御茶ノ水・神保町店** | 御茶ノ水 徒歩5分 | 独自オンライン予約 | **カテゴリB (要認証Web)** | **【中・取得可能】**<br>公式オンライン予約システムあり。会員登録・ログイン後のスケジュール参照が可能。 |
| **BECKアキバ (スタジオ301)** | 秋葉原 徒歩1分 | Googleカレンダー / フォーム / 電話 | **カテゴリD (申請・非即時)** | **【低】**<br>ライブ・イベント兼用。スタジオ単体のリアルタイムカレンダーではなく、問い合わせまたは電話ベース。 |

---

### 6.2 渋谷エリア（カバー率：約 100%）
都内屈指の激戦区。ノア4店舗、ゲートウェイ、ペンタ3店舗で市場のほぼ全域を占有。

| スタジオ名 | 最寄駅 | 予約方式 | カテゴリ | 空き枠取得の実現可能性 / 技術仕様 |
|---|---|---|---|---|
| **サウンドスタジオノア 渋谷本店** | 渋谷 徒歩6分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【即時連携可能】** 全13部屋。共通ノアAPI（`store_id: shibuya_honten`）。 |
| **サウンドスタジオノア 渋谷1号店** | 渋谷 徒歩3分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【即時連携可能】** 全10部屋。共通ノアAPI（`store_id: shibuya1`）。 |
| **サウンドスタジオノア 渋谷2号店** | 渋谷 徒歩5分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【済・実データ検証完了】** 全14部屋。共通ノアAPI（`store_id: shibuya2`）。 |
| **サウンドスタジオノア 渋谷3号店** | 渋谷 徒歩5分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【即時連携可能】** 全8部屋。共通ノアAPI（`store_id: shibuya3`）。 |
| **ゲートウェイスタジオ 渋谷道玄坂店** | 渋谷 徒歩4分 | Reserve1 (ASP) | **カテゴリA (公開Web)** | **【★新発見・即時連携可能】** 全10部屋。`lc=tlsccmeco&mn=8`。Goodmanと全く同一のReserve1基盤のため、完全ログイン不要で日別30分枠テーブルをそのまま取得可能。 |
| **スタジオペンタ 渋谷シティサイド店** | 渋谷 徒歩3分 | 電話受付のみ | **カテゴリC (電話のみ)** | **【即時連携可能】** 全7部屋。1カード集約＋ワンタップ電話発信（`03-3462-2811`）。 |
| **スタジオペンタ 渋谷ジュークハウス店** | 渋谷 徒歩4分 | 電話受付のみ | **カテゴリC (電話のみ)** | **【即時連携可能】** 全7部屋。1カード集約＋ワンタップ電話発信（`03-3462-2815`）。 |
| **スタジオペンタ 渋谷ムーンサイド店** | 渋谷 徒歩5分 | 電話受付のみ | **カテゴリC (電話のみ)** | **【即時連携可能】** 全6部屋。1カード集約＋ワンタップ電話発信（`03-3462-2818`）。 |

---

### 6.3 新宿エリア（カバー率：約 90%超）
巨大ターミナル。ノア新宿店（21室）、ペンタ（計27室）、STUDIO NODE（7室）が主軸。

| スタジオ名 | 最寄駅 | 予約方式 | カテゴリ | 空き枠取得の実現可能性 / 技術仕様 |
|---|---|---|---|---|
| **サウンドスタジオノア 新宿店** | 新宿 徒歩4分 / 西新宿 徒歩2分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【即時連携可能】** 全21部屋の巨大旗艦店。共通ノアAPI（`store_id: shinjuku`）。 |
| **STUDIO NODE 新宿店** | 新宿 徒歩6分 / 西武新宿 徒歩3分 | 自社Web予約 (ASP) | **カテゴリA (公開Web)** | **【★即時連携稼働中】** 全7部屋（00分開始4室・30分開始3室）。`lc=llcvcamtc&mn=1&gr=3`。完全ログイン不要で21日分の空き枠を自動取得中。 |
| **スタジオペンタ 新宿店** | 新宿 徒歩3分 / 新宿三丁目 徒歩2分 | 電話受付のみ | **カテゴリC (電話のみ)** | **【即時連携可能】** 全19部屋（東口の超大型店）。1カード集約＋電話発信（`03-3351-3140`）。 |
| **スタジオペンタ 新宿南口店** | 新宿 徒歩3分 | 電話受付のみ | **カテゴリC (電話のみ)** | **【即時連携可能】** 全8部屋。1カード集約＋電話発信（`03-3350-5020`）。 |
| **リンキィディンクスタジオ 新宿店** | 新宿 徒歩8分 | 独自オンライン予約 | **カテゴリB (要認証Web)** | **【店頭登録完了後に対応】** ライブハウスNine Spices併設。 |

---

### 6.4 高田馬場・池袋エリア（カバー率：100% ★全スタジオWeb即時連動）
学生・音大生・若手バンドマンが最も密集する山手線屈指のエリア。驚異的なことに全スタジオが既存システムでWeb即時連動可能。

| スタジオ名 | 最寄駅 | 予約方式 | カテゴリ | 空き枠取得の実現可能性 / 技術仕様 |
|---|---|---|---|---|
| **ベースオントップ 高田馬場店** | 高田馬場 徒歩1分 | スタジオル (studiol.jp) | **カテゴリA (公開Web)** | **【即時連携可能】** 全10部屋。スタジオルAPI（`shop_id: 703`）。 |
| **サウンドスタジオノア 高田馬場店** | 高田馬場 徒歩3分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【即時連携可能】** 全12部屋。共通ノアAPI（`store_id: takadanobaba`）。 |
| **ゲートウェイスタジオ 高田馬場3号店** | 高田馬場 徒歩2分 | Reserve1 (ASP) | **カテゴリA (公開Web)** | **【即時連携可能】** 全8部屋。Reserve1（`lc=tlsccmeco&mn=4`）。 |
| **ベースオントップ 新池袋西口店** | 池袋 徒歩1分 | スタジオル (studiol.jp) | **カテゴリA (公開Web)** | **【即時連携可能】** 全11部屋。スタジオルAPI（`shop_id: 701`）。 |
| **サウンドスタジオノア 池袋店** | 池袋 徒歩4分 | ノア公式Web予約 | **カテゴリB (要認証Web)** | **【即時連携可能】** 全14部屋。共通ノアAPI（`store_id: ikebukuro`）。 |
| **ゲートウェイスタジオ 池袋北口店** | 池袋 北口徒歩2分 | Reserve1 (ASP) | **カテゴリA (公開Web)** | **【実装済・稼働確認済 2026-09-17】** 全16部屋 (5F/6F統合)。Reserve1（`lc=tlsccmeco&mn=3&gr=1`）。 |
| **スタジオペンタ 池袋店 / ハンズサイド** | 池袋 徒歩3分 | 電話受付のみ | **カテゴリC (電話のみ)** | **【即時連携可能】** 各7〜8部屋。1カード集約＋電話発信。 |

---

### 6.5 山手線東側エリア（品川・新橋・上野・大塚）＆城東エリアの分析
山手線東側（東京・有楽町・新橋・品川〜上野・日暮里）は、西側（新宿・渋谷等）と比べてビジネス街・官公庁街の性格が強いため、スタジオの絶対数は限られますが、要所要所に強力な拠点が点在しています。

| エリア・拠点 | スタジオ名 | 予約方式 | カテゴリ | 特徴とシステム対応状況 |
|---|---|---|---|---|
| **秋葉原・神田** | ノア、BOT、GOODMAN、音楽館 | Web混在 | **A / B** | **東側最大の音楽コア拠点**。全4系列・全36部屋を完全網羅済み。 |
| **大塚・巣鴨・駒込** | **スタジオ音楽館 大塚駅前店**<br>**スタジオペンタ 大塚店**<br>STUDIO DANG DANG 駒込 | ajg.jp<br>電話<br>audia.jp | **A-<br>C<br>A** | ・音楽館大塚店は秋葉原と同じ `ajg.jp` で**Web即時連動可能**。<br>・ペンタ大塚店は電話集約モデルで即座に対応可能。<br>👉 **大塚エリアのカバー率は約80〜90%**。 |
| **銀座・新橋** | **サウンドスタジオノア 銀座店** | ノア公式Web予約 | **B** | 銀座駅徒歩2分・新橋駅徒歩6分。全9部屋。既存のノアAPI（`store_id: ginza`）で**Web即時連動可能**。 |
| **品川・五反田** | **スタジオペンタ 五反田店** | 電話受付のみ | **C** | 品川駅至近のバンド需要を一手に引き受ける老舗。電話集約で対応可能。 |
| **城東（錦糸町・小岩・北千住・柏・千葉）** | スタジオペンタ 千葉/船橋<br>スタジオ音楽館 柏店<br>ゲートウェイスタジオ 柏店<br>studio PACKS 北千住 | 電話<br>ajg.jp<br>Reserve1<br>独自 | **C<br>A-<br>A<br>B** | 山手線東側から東へ延びる総武線・常磐線沿線にスタジオ文化が展開。<br>柏（音楽館・GW）、千葉（ペンタ）など、既存の3大基盤（Reserve1, ajg.jp, ペンタ）でカバー可能。 |

---

### 6.6 吉祥寺・下北沢・東急沿線エリア
| エリア | 主要スタジオ構成 | カバー率 | 状況 |
|---|---|:---:|---|
| **吉祥寺** | ノア 吉祥寺店 (13室)<br>ペンタ 吉祥寺店/サウスサイド店 (計16室)<br>リンキィディンク 吉祥寺店 | **75〜80%** | ノア（Web○）＋ペンタ2店（電話）で即座に大半を網羅。 |
| **下北沢** | ノア 下北沢店<br>リンキィディンク 下北沢1st/2nd<br>スタジオベアーズ | **50〜60%** | ノアは即時対応。リンキィディンク店頭登録完了後に90%超へ。 |
| **東急・世田谷（都立大/自由が丘/学芸大/三茶）** | ノア 都立大本店/自由が丘/学芸大/駒沢/三軒茶屋<br>ペンタ 自由が丘店 | **90〜95%** | ノアの創業地・最大拠点群。ノアAPIだけで50室以上をWeb連動可能。 |

---

#### カテゴリ定義
- **カテゴリA（公開Webカレンダー）**: ログイン不要でカレンダーや枠状況が一般公開されており、最も安全・高速に連携可能（Reserve1, スタジオル等）。
- **カテゴリB（要認証Web予約システム）**: 会員ログインやCookieセッションが必要だが、APIや画面解析で自動取得可能（ノア等）。
- **カテゴリC（電話・店頭のみ）**: Webシステムなし。ダミー枠を廃止し、1スタジオ1カードで電話予約へ誘導（ペンタ等）。
- **カテゴリD（申請型・イベント兼用）**: リアルタイムな空き枠カレンダーがなく、メールやフォームでの折り返し確定。

---

## 7. 各スタジオの会員登録状況＆最長予約枠・クローリング期間対照表

### 7.1 会員登録状況＆システム特性一覧

各スタジオの予約システムに対する**会員登録ステータス**、**閲覧時の認証要否**、**システム特性・登録要件**の管理一覧です。

| 予約システム種別 | 導入スタジオ例 | 閲覧時の要否 | 取得技術仕様 | 登録要件・システム特性 |
|---|---|:---:|---|---|
| **独自Web予約システム<br>(book.studionoah.jp)** | **サウンドスタジオノア**<br>(秋葉原, 渋谷4店, 新宿, 池袋, 高田馬場, 銀座, 都立大等) | 要ログイン | 内部API (JSON)<br>Cookieセッション | ・Web上で本登録が完結可能<br>・ログインセッション（`storageState.json`）による一括API取得が稼働中<br>・都内最大手（00分/30分開始混在） |
| **Reserve1.jp<br>(Reserve Mart ASP)** | **STUDIO GOODMAN AKIBA**<br>**ゲートウェイスタジオ各店** (渋谷, 池袋, 高田馬場等)<br>**STUDIO NODE各店** (新宿, 水道橋等) | **不要 (公開)** | HTMLスクレイピング<br>(EUC-JP) | ・「ゲスト様」として完全ログイン不要で日別30分枠テーブルを一般閲覧可能<br>・チェックボックス（`☐`）= 空き、`×` = 予約済で100%機械判定可能<br>・同一パーサーで都内中堅チェーンを網羅可能 |
| **スタジオル<br>(studiol.jp)** | **ベースオントップ各店**<br>(秋葉原, 新池袋西口, 高田馬場等) | **不要 (公開)** | JSON API<br>(get_schedule_shop) | ・誰でもログイン不要で全店・全部屋の空きカレンダーを一般閲覧可能<br>・全国規模の予約ASP。91日先まで取得可能 |
| **ajg.jp<br>(独自PHPシステム)** | **スタジオ音楽館各店**<br>(アキバ店, 大塚駅前店, 柏店等) | **不要 (公開)** | HTMLスクレイピング<br>(sescode自動発行) | ・セッションコード自動発行によりログイン不要でカレンダー閲覧可能<br>・1時間ごとの空き枠テーブルをパース |
| **独自オンライン予約<br>(rinky.info)** | **リンキィディンクスタジオ**<br>(下北沢, 新宿, 高円寺, 吉祥寺, 御茶ノ水等) | 要ログイン | 画面解析 (HTML) | ・Web仮登録後、**店頭での身分証確認・本登録手続きが必須**<br>・現地登録完了後にセッション連携を検証予定 |
| **電話・店頭受付のみ<br>(Web予約なし)** | **スタジオペンタ各店**<br>(渋谷3店, 新宿2店, 池袋2店, 大塚, 吉祥寺2店, 千葉等) | 対象外 | 静的マスター管理<br>(tel: 発信CTA) | ・Web予約システム自体が存在しない（全店舗共通）<br>・公式HPスペックを取得し「1スタジオ1カード」＋電話発信CTAで運用 |
## 7. スタジオ別クローリング・スライディングウィンドウ設計

### ① 各スタジオの最長予約受付期間 vs クローリング取得日数

| スタジオ名 | エリア | 予約システム種別 | 一般バンド予約 受付可能期間 / 個人練習受付 | クローラー取得日数 (スライディングウィンドウ) | 取得仕様・スロット数 |
|---|---|---|---|---|---|
| **サウンドスタジオノア 渋谷本店** | 渋谷 | ノア独自Web API | **3ヶ月前の1日〜** / 前日21:00〜 | **直近 21日間 (3週間分)** | `render_chart` APIで一括返却 |
| **サウンドスタジオノア 渋谷1号店** | 渋谷 | ノア独自Web API | **3ヶ月前の1日〜** / 前日21:00〜 | **直近 21日間 (3週間分)** | `render_chart` APIで一括返却 |
| **サウンドスタジオノア 渋谷2号店** | 渋谷 | ノア独自Web API | **3ヶ月前の1日〜** / 前日21:00〜 | **直近 21日間 (3週間分)** | `render_chart` APIで一括返却 |
| **サウンドスタジオノア 渋谷3号店** | 渋谷 | ノア独自Web API | **3ヶ月前の1日〜** / 前日21:00〜 | **直近 21日間 (3週間分)** | `render_chart` APIで一括返却 |
| **サウンドスタジオノア 新宿店** | 新宿 | ノア独自Web API | **3ヶ月前の1日〜** / 前日21:00〜 | **直近 21日間 (3週間分)** | `render_chart` APIで一括返却 |
| **サウンドスタジオノア 秋葉原店** | 秋葉原 | ノア独自Web API | **3ヶ月前の1日〜** / 前日21:00〜 | **直近 21日間 (3週間分)** | `scripts/lib/noah-fetcher.ts`（全14部屋・部屋マスターは `src/config/noah-master.ts`） |
| **ゲートウェイスタジオ 渋谷道玄坂店** | 渋谷 | Reserve1.jp (公開Web) | **3ヶ月前の同日〜** / 前日09:00〜 | **直近 21日間 (3週間分)** | 軽量Node fetch（3,528スロット取得） |
| **STUDIO NODE 新宿店** | 新宿 | studio-node.jp (公開Web) | **2ヶ月前の同日〜** / 3日前〜 | **直近 21日間 (3週間分)** | 軽量Node fetch（全7部屋・2,058スロット完全同期） |
| **STUDIO GOODMAN AKIBA** | 秋葉原 | Reserve1.jp (公開Web) | **2ヶ月前の同日〜** / 前日〜 | **直近 21日間 (3週間分)** | 軽量Node fetch（4,670スロット取得） |
| **ベースオントップ 秋葉原昭和通り口店** | 秋葉原 | スタジオル (公開API) | **最大 91日先 (約3ヶ月)** / 前日〜 | **直近 21日間 (3週間分)** | 軽量Node fetch（3,024スロット取得） |
| **スタジオ音楽館 アキバ店** | 秋葉原 | ajg.jp (公開PHP) | **公開画面上 14日先**<br>(電話・店頭は2ヶ月前) / 前日21:00〜 | **直近 21日間 (3週間分)** | `scripts/lib/ongakukan-fetcher.ts`（全7部屋・3,528スロット完全同期） |
| **スタジオペンタ 渋谷シティサイド店** | 渋谷 | 電話受付のみ | **2ヶ月前〜** / 前日10:00〜 | **対象外 (0日・tel:誘導)** | Web予約なし・ワンタップ電話発信 |
| **スタジオペンタ 渋谷ジュークハウス店** | 渋谷 | 電話受付のみ | **2ヶ月前〜** / 前日10:00〜 | **対象外 (0日・tel:誘導)** | Web予約なし・ワンタップ電話発信 |
| **スタジオペンタ 渋谷ムーンサイド店** | 渋谷 | 電話受付のみ | **2ヶ月前〜** / 前日10:00〜 | **対象外 (0日・tel:誘導)** | Web予約なし・ワンタップ電話発信 |
| **スタジオペンタ 新宿店** | 新宿 | 電話受付のみ | **2ヶ月前〜** / 前日10:00〜 | **対象外 (0日・tel:誘導)** | Web予約なし・ワンタップ電話発信 |
| **スタジオペンタ 新宿南口店** | 新宿 | 電話受付のみ | **2ヶ月前〜** / 前日10:00〜 | **対象外 (0日・tel:誘導)** | Web予約なし・ワンタップ電話発信 |

#### 💡 なぜ最長予約枠（2〜3ヶ月先）まで全日クローリングしないのか？（スライディングウィンドウ設計の意図）
1. **予約・検索需要の集中ゾーン（直近1〜2週間が全体の8割以上）**:
   - リハーサルスタジオの空き枠検索において、ユーザーが「今すぐ枠を押さえたい」需要の大半は**「今日」「明日」「今週末」「来週」などの直近1〜2週間に集中**しています。
   - 2〜3ヶ月先の枠は「ワンマンやツアーのゲネプロ予約」など公式Webから長期的計画で直接押さえるケースが多く、直前枠ほど頻繁なキャンセル・空き枠変動が発生しません。
2. **相手サーバーへの負荷軽減とBAN回避（人間化ポリシー遵守）**:
   - 全16店舗に対して90日分（3ヶ月分）の全カレンダーを毎時クローリングすると、1回のクローラーで**数千回〜1万回以上のHTTPリクエスト**が発生し、相手サーバーへ多大な負荷をかけ、WAFやbot検知システム（Cloudflare等）によるIP/アカウント制限のリスクが跳ね上がります。
3. **GitHub Actions実行時間と無料枠の維持**:
   - 90日分を巡回すると1回の実行時間が10〜15分に達し、GitHub無料枠（2,000分/月）を瞬時に超過します。
   - したがって、**「予約変動が最も激しい直近1〜3週間」をスライディングウィンドウ（日次更新）で集中的・穏やかに巡回する**設計が、サーバー負荷・コスト・ユーザー利便性のすべてにおいて最も最適なバランスを実現しています。

---

## 8. 主要ファイル・コード構成マップ

| パス | 種別 | 役割・概要 |
|---|---|---|
| `docs/SYSTEM_DESIGN_AND_SPECS.md` | ドキュメント | **本ドキュメント**（設計仕様・運用ルール・エリア調査マップ） |
| `src/config/supported-studios.ts` | 設定 (マスター) | **都内22店舗（渋谷・新宿・秋葉原・高田馬場）の対応スタジオ定義データ**（エリア、部屋数、連携方式、設備、営業時間、連絡先） |
| `scripts/crawl-studios.ts` | スクリプト | **自動定期巡回クローラー統合エントリーポイント**（渋谷・新宿・秋葉原・高田馬場の全スタジオをスライディングウィンドウ21日間で自動巡回・DB/JSON同期。ただしNOAHはGitHub ActionsのIPブロックによりローカル実行限定） |
| `src/lib/gateway-takadanobaba-converter.ts` | データ変換 | ゲートウェイ高田馬場3号店（15部屋）の実データコンバーター |
| `src/lib/ongakukan-takadanobaba-converter.ts` | データ変換 | スタジオ音楽館 馬場駅前店（6部屋）の実データコンバーター |
| `src/lib/bot-takadanobaba-converter.ts` | データ変換 | BASS ON TOP 高田馬場店（11部屋）の実データコンバーター |
| `src/lib/bazooka-converter.ts` | データ変換 | BAZOOKA STUDIO（6部屋・自動巡回非対応の静的リスティング）のコンバーター |
| `scripts/lib/id-utils.ts` | モジュール (単一の真実源) | **`rooms.id` / `availability_slots.room_id`共通の決定的UUID採番関数**（`toUUID(canonicalId)`）。`scripts/crawl-studios.ts`と`supabase/seed.sql`生成の両方がこの関数の入出力に一致させる前提 |
| `scripts/crawl-ongakukan-shinjuku.ts` | スクリプト | **スタジオ音楽館 新宿西口店専用巡回スクリプト**（ajg.jp / 21日間全7部屋自動パース） |
| `scripts/lib/reserve1-fetcher.ts` | モジュール | **Reserve1 ASP用超軽量スクレイパー**（Node fetch / GOODMAN、ゲートウェイ渋谷） |
| `scripts/lib/bot-fetcher.ts` | モジュール | **BASS ON TOP（スタジオル）用超軽量スクレイパー**（Node fetch / 秋葉原昭和通り口店） |
| `scripts/lib/ongakukan-fetcher.ts` | モジュール | **スタジオ音楽館（ajg.jp）用超軽量スクレイパー**（Node fetch / アキバ店・新宿西口店両対応・全室21日間自動パース） |
| `src/config/noah-master.ts` | 設定 (マスター・単一の真実源) | **ノア全7店舗・101部屋の部屋マスター**（`studioId`・畳数・開始オフセット・ログイン要否・実料金・実機材）。`scripts/lib/noah-fetcher.ts`（クロール）と `src/lib/noah-tokyo-converter.ts`（表示）の双方がここを読み込む |
| `scripts/lib/noah-fetcher.ts` | モジュール | **サウンドスタジオノア用スクレイパー**（公式Schedule API / 都内全7店舗・101部屋・21日間自動パース。部屋マスターは `src/config/noah-master.ts` を参照） |
| `scripts/lib/node-fetcher.ts` | モジュール | **STUDIO NODE 新宿店用スクレイパー**（Node fetch / 全7部屋・00分/30分開始混在・21日間自動パース） |
| `scripts/lib/penta-fetcher.ts` | モジュール | **スタジオペンタ新宿店用スクレイパー**（Supabase Edge Functions KV-API / 全19部屋・土日祝リアルタイム空き状況自動パース） |
| `src/data/penta-shinjuku-real.json` | データ | スタジオペンタ新宿店 全19部屋・土日祝日の実データ（機材・料金・リアルタイム空き状況スロット） |
| `src/lib/penta-converter.ts` | コンバーター | ペンタ全4店舗のマスター定義および新宿店の土日祝リアルタイムスロット/平日電話案内ハイブリッド変換ロジック |
| `src/data/ongakukan-shinjuku-real.json` | データ | スタジオ音楽館 新宿西口店 全7部屋・21日間の実データ（機材・料金・リアル空き枠 3,528スロット） |
| `src/lib/ongakukan-shinjuku-converter.ts` | コンバーター | スタジオ音楽館 新宿西口店の実データをアプリ共通の `RoomWithSlots` 形式へ正規化変換するロジック |
| `src/data/node-shinjuku-real.json` | データ | STUDIO NODE 新宿店 全7部屋・21日間の実データ（機材・料金・リアル空き枠 2,058スロット） |
| `src/lib/node-converter.ts` | コンバーター | STUDIO NODE 新宿店の実データをアプリ共通の `RoomWithSlots` 形式へ正規化変換するロジック |
| `src/data/akihabara-real.json` | データ | 秋葉原3スタジオ（BOT、GOODMAN、音楽館）全22部屋の100%実データ（機材・料金・リアル空き枠）。NOAH秋葉原店は `src/config/noah-master.ts` / `src/data/noah-tokyo-real.json` 側に完全統合済み |
| `src/lib/akiba-converter.ts` | コンバーター | 秋葉原実データJSONをアプリ共通の `RoomWithSlots` 形式へ正規化変換するロジック |
| `src/data/gateway-shibuya-real.json` | データ | ゲートウェイスタジオ渋谷道玄坂店 全12部屋・21日間の実データ（機材・料金・リアル空き枠 3,528スロット） |
| `src/lib/gateway-converter.ts` | コンバーター | ゲートウェイ渋谷実データJSONをアプリ共通の `RoomWithSlots` 形式へ正規化変換するロジック |
| `src/data/noah-tokyo-real.json` | データ | ノア都内全7店舗（渋谷4店、新宿1店、秋葉原2店）全101部屋・21日間の実データ |
| `src/lib/noah-tokyo-converter.ts` | コンバーター | ノア全7店舗（秋葉原店含む）の実データをアプリ共通の `RoomWithSlots` 形式に正規化変換するロジック。部屋マスター・実料金・実機材は `src/config/noah-master.ts` を参照 |
| `src/lib/mock-data.ts` | データソース | 全スタジオの統合データハブ。ダミーデータを全廃し、都内実店舗の実データ・正規スペックのみを配信 |
| `src/data/museum-shinjuku-real.json` / `hillvalley-real.json` / `vantage-real.json` | データ | 新宿エリア追加分3店舗（studi-ol.com自動同期）の全部屋・22日間の実データ |
| `src/lib/shinjuku-studiol-converter.ts` | コンバーター | スタジオミュージアム新宿店・ヒルバレースタジオ・Sound Studio Vantageの実データJSONを `RoomWithSlots` 形式へ正規化変換するロジック（下北沢のstudi-ol系コンバータと同じBASS::/DRUM::プレフィックス方式） |
| `src/data/music-man-real.json` | データ | Music man サウンドスタジオ（Reserve1自動同期）全10部屋・22日間の実データ |
| `src/lib/music-man-converter.ts` | コンバーター | Music manの実データJSONを `RoomWithSlots` 形式へ正規化変換するロジック |
| `src/data/sound-studio-dom-real.json` / `pig-studio-real.json` / `sonic-band-studio-real.json` / `koyama-main-real.json` / `koyama-r-real.json` / `musira-real.json` | データ | 高円寺エリア追加分6店舗（studi-ol.com自動同期）の全部屋・22日間の実データ |
| `src/lib/koenji-studiol-converter.ts` | コンバーター | 上記6店舗の実データJSONを `RoomWithSlots` 形式へ正規化変換するロジック（BASS::/DRUM::/NOTE::プレフィックス方式） |
| `src/data/studio-bayd-koenji-real.json` | データ | STUDIO BAYD 高円寺店（WnSpaceMusic公開API自動同期）全5部屋・22日間の実データ |
| `src/lib/studio-bayd-converter.ts` | コンバーター | STUDIO BAYD 高円寺店の実データJSONを `RoomWithSlots` 形式へ正規化変換するロジック |
| `scripts/lib/wnspace-fetcher.ts` | フェッチャー | WnSpaceMusic（STUDIO BAYDチェーン等）向け。ログイン不要の公開REST API (`/api/studios/bookings`) から確定予約一覧を直接取得し、24時間分の空き/予約済みを算出する（HTMLスクレイピング不要） |
| `src/data/studiosun-nishifunabashi-real.json` | データ | STUDIO SUN 西船橋店（webtoru.com自動同期）全7部屋・22日間の実データ |
| `scripts/lib/webtoru-fetcher.ts` | フェッチャー | webtoru.com（ウェブトル）向け。ログイン不要のPOST APIから日別タイムラインHTMLを取得し、絶対配置バー（予約済み/営業時間外）の位置から空き状況を算出する |
| `src/lib/funabashi-converter.ts` | コンバーター | STUDIO SUN 西船橋店の実データJSONを `RoomWithSlots` 形式へ正規化変換、およびスタジオパックス船橋店（会員ログイン必須のため静的リスティング、10部屋）を定義するロジック |
| `src/components/timeline/studio-timeline-view.tsx` | UI | 26列グリッドによる30分開始枠の物理シフトタイムライン表示 |
| `src/components/search/studio-card.tsx` | UI | スタジオ・部屋の一覧カード。空き状況や電話予約CTAの動的切り替え |
| `src/components/studio/room-detail-modal.tsx` | UI | 部屋詳細モーダル（常設機材、帖数、個人練習料金、予約リンク/電話発信） |
| `src/config/banner-ads.ts` | 設定 | A8.net画像バナー広告の一元管理（広告主・画像URL・クリックURL・トラッキングピクセル、サイドバー用/横長バナー用の抽出フィルタ） |
| `src/components/search/sidebar-banner-ad.tsx` | UI | PCサイドバー専用の画像バナー枠（8秒ローテーション、開始位置ずらしで2枠併設可能） |
| `src/components/common/horizontal-banner-ad.tsx` | UI | 横長画像バナー1点の表示専用コンポーネント（左上に小さく「PR」を重ねるミニマル表示） |
| `src/components/common/fixed-bottom-ad-banner.tsx` | UI | 画面下部に常時固定表示する横長バナー（PC・スマホ共通、8秒ローテーション） |
| `src/lib/supabase/client.ts` | インフラ | Supabase クライアント定義（`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`） |
| `src/lib/supabase/api.ts` | データ層 | Supabase からスタジオ・部屋・機材・空き枠スロットを一括結合取得し `RoomWithSlots` へ変換するAPI層 |
| `src/app/api/studios/route.ts` | APIエンドポイント | `/api/studios` エンドポイント。Supabaseリアル空き枠データを最優先配信（フォールバック付き） |
| `supabase/migrations/20260905000000_initial_schema.sql` | DB設計 | 本番SupabaseデータベースDDL定義（studios, rooms, room_equipments, availability_slots, RLSポリシー） |
| `.github/workflows/crawl-studios.yml` | CI/CD (自動化) | **GitHub Actions完全クラウド自動巡回ワークフロー**（30分おきcron＋手動実行・差分自動コミット＋Supabaseシークレット注入） |
| `scripts/crawl-studios.ts` | クローラー | 公開スタジオ（Reserve1等）の一括巡回、Supabaseへのリアルタイム直接同期、ノアAPIステルス疎通バッチ |
| `.env.local` | 設定 (秘匿) | Supabase接続設定（`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`）およびノア認証情報 |
| `src/lib/gtag.ts` | ユーティリティ | **Google Analytics (GA4) 計測モジュール**（ページビュー送信、公式予約クリック・電話発信クリック等のカスタムイベントトラッキング） |
| `storageState.json` | 設定 (秘匿) | ノアの認証済みセッションCookie保存ファイル |

---

## 9. アクセス解析・イベント計測設計 (Google Analytics 4)

ユーザーの利用動向や各スタジオへの送客効果を正確に可視化するため、**Google Analytics 4 (GA4)** を導入し、Next.js 15（App Router）の標準 `<Script>`（`afterInteractive`）でページ表示速度を犠牲にすることなく非同期読み込みを行います。

### 9.1 計測パラメータ仕様
- **測定ID**: `G-1X4KN493Q7`（環境変数 `NEXT_PUBLIC_GA_ID` で上書き・管理可能）
- **読み込み方針**:
  - `src/app/layout.tsx` の `<head>` 内で `afterInteractive` 戦略により遅延非同期ロード。
  - 初期PV（Pageview）を自動計測。

### 9.2 カスタムイベント計測一覧
アプリ内の重要アクションを以下の仕様で自動トラッキングします：

| イベント名 (`action`) | カテゴリ (`category`) | ラベル (`label`) | トリガー契機 | 分析目的 |
|---|---|---|---|---|
| `click_official_booking` | `booking` | `{スタジオ名}` (例: `サウンドスタジオノア 渋谷1号店`) | スタジオカードの「公式WEB予約を開く ↗」をクリック | 各スタジオ公式予約サイトへの送客数・スタジオ別需要の可視化 |
| `click_phone_call` | `booking` | `{スタジオ名} ({電話番号})` (例: `スタジオペンタ 渋谷店 (03-3461-0000)`) | 電話専用スタジオカードの「電話で予約」ボタンをクリック | 電話問い合わせ件数の計測 |

